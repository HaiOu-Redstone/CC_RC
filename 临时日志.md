# CC_RC 临时日志

> 用途：简略记录每次模组操作，只追加、不修改已有内容。
> 每次打包新版本时，本日志内容将作为该版本的更新说明发布，随后归档到 `临时日志归档/` 并新建空白日志继续记录。

## 2026-09-20

- 【打包 v0.0.10】按「打包发布流程.md」执行：`gradle.properties` mod_version 0.0.9→0.0.10（上一版内容含：数据单元/方块探测器/说明书2章节/贴图模型资源整理/专属贴图/继电器输入输出方向两轮修正/FAAS 音频两轮修复/反应堆启动音乐/控制面板文字样式+微调）；gradlew build BUILD SUCCESSFUL（38s），产物 `build/libs/cc_rc-0.0.10.jar`；本周期全部临时日志条目（12 条）已归档至 `临时日志归档/临时日志-v0.0.10.md` 并将作为 Release 说明；开发日志本周期内容已同步到对应章节（二、19/21/29/33/34/35/36、目录 52~55、三、4 声音 38）
- 【发布 v0.0.10 完成】git 提交 dcd55db（183 文件，+1364/−220）已推送 `main`（SSH over 443）；`gh release create v0.0.10` 成功：https://github.com/HaiOu-Redstone/CC_RC/releases/tag/v0.0.10，Release 说明采用归档临时日志内容（12 条），附件 `build/libs/cc_rc-0.0.10.jar`
- 【注册海报1方块 poster_1】素材 `模型\其他物品\海报\D54EAA128BA11D86C7C1AE19BA63D132.png`（2667x2667 正方图，System.Drawing 双三次缩放 **256x256** 存入 `textures/block/poster/poster_1.png`）：①新类 [PosterBlock] extends `HorizontalDirectionalBlock`——**只能贴墙**（`getStateForPlacement` 仅点击水平墙面时 `FACING=clickedFace` 四向，点顶/底面返回 null）；**碰撞箱 16x16x1**（NORTH=box(0,0,15,16,16,16)，按 FACING 旋转对应）；`canSurvive` 查 `pos.relative(FACING).isSolid()`、`updateShape` 照抄原版 WallSignBlock（墙没了转 AIR 掉落）；属性 `noCollission+strength(0.5F)+木质音效`；②**模型**：1px 板单元素（front=north 面显示图片、背面/侧边不渲染），blockstates 四向 `y=0/90/180/270`（以 north 为基准，方向语义同原版 WallSign），item model parent 引用方块模型；③注册 ModBlocks POSTER_1 / ModItems POSTER_1_ITEM / 创造栏 / lang 中英「海报1 / Poster 1」；④gradlew build BUILD SUCCESSFUL（43s）；开发日志新增二、37 与目录 56，三、1 方块 109→110、三、2 物品 136→137（方块物品 75→76）
- 【海报1修复（透明 + 支撑检测方向）】用户实测反馈两处：①**贴图透明**——原图带 alpha，海报显示有透明/黑块；已用 System.Drawing 以白底重绘去 alpha（`Clear(White)` + `DrawImage`），逐像素采样验证全部不透明（1369/1369 采样全 alpha=255）；②**支撑检测方向错误**——`canSurvive` 初版写 `pos.relative(FACING)`，而支撑墙实际在 **FACING 反方向**（原版 WallSignBlock 为 `pos.relative(FACING.getOpposite())`），导致"点击墙时需墙对侧两格处也有方块才能放置"；已修正为 `pos.relative(FACING.getOpposite())` 并同步类注释方向语义（用户确认放置面朝向本身正确，仅检测面错）；gradlew build BUILD SUCCESSFUL（29s）；开发日志二、37 已同步修正
- 【海报1改为保留透明（修正上一轮）】用户澄清：**不需要白色背景，要求保持透明**（海报是异形透明贴图）。①贴图：重新双三次缩放原图**保留 alpha 通道**（不再 Clear(White) 填底），采样验证 532 不透明/19 半透明（缩放边缘羽化）/818 全透明；②渲染：`CcRc.onClientSetup` 为 `POSTER_1` 注册 `RenderType.cutout()`——透明部分真正透明而非 solid 渲染变黑（与核弹按钮/钥匙柜同款处理），项目既有 removal 警告 +1；gradlew build BUILD SUCCESSFUL（32s）；开发日志二、37 素材说明与渲染说明已改回（注明初版误填白底、用户要求后改回）
- 【海报1物品改为平面图】用户要求物品形态不用方块模型、直接显示图片：①新增物品贴图 `textures/item/poster_1.png`（复制方块贴图，透明保留，256x256）；②`models/item/poster_1.json` 由 parent 方块模型（1px 板）改为 `minecraft:item/generated` + `layer0: cc_rc:item/poster_1`（经典 2D 物品图标，物品栏/手持/掉落物直接显示海报平面图）；方块放置显示不受影响（仍为贴墙 1px 板 + cutout）；gradlew build BUILD SUCCESSFUL（29s）；开发日志二、37 与三、2 物品表已同步
- 【批量注册海报 2~16】素材 `模型\其他物品\海报` 现有 **15 张**新海报（1~8.png 方形 + ask_yourself/energy/forgetting/parking_lot/see_more/than_just_you/we_need_you 竖版；已比对确认 1.png 与海报1 原图 D54EAA 不同，全部为新注册）。**命名**：按文件名排序 poster_2~16。**贴图**（双三次缩放保留 alpha）：方形 8 张 block 256x256 全图；竖版 7 张 block **256x512 画布内容底对齐**（787x1024→内容 256x333、y0=179；1654x2339→内容 256x362、y0=150）保证底贴格子底、顶边超出且比例不变；item 贴图方形复制、竖版生成 256x256 方形画布内容居中（防 16x16 图标压扁）。**模型**：方形同海报1（高16 UV0~16）；竖版 to.y=20.8125（787x1024）/ 22.625（√2 的 parking_lot），UV v 起 5.59375/4.6875（内容顶在画布位置），**碰撞箱保持 16x16x1 不变**（用户明确要求，仅渲染超出）。**注册**：ModBlocks 循环 `POSTERS`（15 注册）、ModItems 循环 `POSTER_ITEMS`、创造栏 forEach、CcRc `POSTERS.forEach` 批量 cutout（保留透明）。lang 中英「海报2~16 / Poster 2~16」。gradlew build BUILD SUCCESSFUL（30s）；开发日志新增目录 57、二、37 子节，三、1 方块 110→125、三、2 物品 137→152（方块物品 76→91）
- 【注册唱片「LEVEL !」】按 ccrc-music-disc skill 流程（感叹号不入注册名，key=level）：①素材 `模型\唱片\level !.mp3` + `level!.png`，VLC 转 **mono 44.1k vorbis 160kbps**（`sounds/music/level.ogg`，1.85MB），校验 ch=1 rate=44100、**ticks=2383**（≈1:59）；贴图复制为 `textures/item/music_disc_level.png`；②ModSounds 注册 `MUSIC_LEVEL`（music_level）、ModItems 注册 `MUSIC_DISC_LEVEL`（**比较器输出 13**——1~15 已全部被占用故复用级别 13，同 reimu；Rarity.RARE stacksTo(1)，ticks 2383）；③sounds.json `music_level`（stream:true）+ subtitle；lang 中英「音乐唱片 / Music Disc」「LEVEL !」subtitle「音乐唱片 - LEVEL ! / Music Disc - LEVEL !」；item model `music_disc_level.json`（item/generated）；原版 tag music_discs 追加 `cc_rc:music_disc_level`（唱片机兼容）；创造栏 MUSIC_DISC_AIR 后加入；④gradlew build BUILD SUCCESSFUL（36s）；开发日志：物品 152→**153**、声音 38→**39**（33 唱片 + 6 非唱片）、唱片表追加 music_disc_level、比较器复用说明 13=reimu、level
- 【注册邪恶盖金 evil_gajin】敌对生物，素材 `模型\生物\邪恶盖金`（evil_gaijin.png 16x16 刷怪蛋贴图 + 6 个中文语音 mp3，复制英文临时名后 VLC 转 mono 44.1k vorbis 160k 存 `sounds/evil_gajin/*.ogg`，rwr 约 3 分钟 3.6MB）。**实体类** `EvilGajin extends Zombie`（僵尸 AI 复用）：不燃烧（isSunBurnTick=false）、不捡装备（canPickUpLoot=false）、100 血 / 20 攻 / 基础速度 0.35；**距离变速**（customServerAiStep 按目标距离设 MOVEMENT_SPEED）：>10 格 0.35（≈15.1 m/s 快于疾跑 5.6）、3.5~10 格 0.28、<3.5 格 0.10（≈4.3 m/s 低于疾跑）。**语音**：锁定瞬间「进攻D点」一次（目标从无到有）、攻击随机「干得好/命中」（doHurtTarget 内播放——1.20.1 Zombie 无 getAttackSound hook）、击杀「摧毁目标」、受击「局势不太妙」（10 秒冷却）。**RWR 循环音**：服务端锁定玩家时发 S2C `EvilGajinRwrPacket`（id 3，开始/停止+实体ID），客户端 `EvilGajinRwrSound`（**AbstractTickableSoundInstance** + looping=true 循环 + tick 跟随实体坐标，实体死亡兜底自停；不能用 MovingSound——1.20.1 不存在；父类 stop() protected → 加公开 requestStop()），管理器按实体 ID 防重复。**模型** EvilGajinModel 复用 GajinModel.createBodyLayer()（泛型要求独立类），渲染器贴图暂用 gajin.png。**刷怪蛋** evil_gajin_spawn_egg（ForgeSpawnEggItem 0x8B0000/0x000000，贴图 evil_gaijin.png 16x16，item/generated），实体不自然生成。编译排错三轮：MovingSound 不存在→AbstractTickableSoundInstance、SoundInstance 在 client.resources.sounds 包、stop() protected→requestStop。gradlew build BUILD SUCCESSFUL（35s）；开发日志目录 58、二、38 新增章节、三、2 物品 153→154、三、4 声音 39→45（唱片 33 + 非唱片 12）、三、6 实体 5→6（Mob 4→5）、唱片表标题 32→33（并修正上一轮 LEVEL 唱片声音段未落位的遗漏）
- 【邪恶盖金微调】用户三点反馈：①**删除原版僵尸音效**——覆盖 getAmbientSound/getHurtSound/getDeathSound 返回 null（僵尸呻吟/受伤/死亡音全静音，只用自定义语音）；②**RWR 追击音太小（被追听不见）**——EvilGajinRwrSound 音量 0.6→**1.0**、衰减 LINEAR→**Attenuation.NONE**（无距离衰减，该音只发给被追玩家）；③**简化速度档位**——去掉 3.5 格内继续降速的中间档，仅两档：>10 格 0.35（快于疾跑）、≤10 格 0.12（≈5.2 m/s 低于疾跑），常量改名 FAST_DIST/SPEED_FAST/SPEED_CLOSE；gradlew build BUILD SUCCESSFUL（33s）；开发日志二、38 已同步微调描述
- 【邪恶盖金语音响度归一化】用户反馈 5 个短语音（lock/good/hit/destroy/bad）音量特别小、小于满音量的 RWR 追击音，怀疑原音频响度低——**实测确认**：素材峰值仅 lock -8.7dB / good -10.2dB / hit -4.9dB / destroy -6.9dB / bad -7.8dB（RMS 更低），确实偏低。处理：①尝试 VLC sout `scales=4` 增益——**实测无效**（wav 探针峰值与无增益完全相同 12059，此版本 VLC 不应用该参数）；②改用可靠方案：**VLC 转 wav(PCM16) → PowerShell 逐样本线性放大（目标峰值 -1dBFS≈29000，上限 4×，按各文件峰值自动计算增益）→ VLC 再转 ogg（mono 44.1k vorbis 160k）**，5 个语音统一归一化到约 -1dBFS（lock +7.6dB / good +9dB / hit +3.8dB / destroy +5.9dB / bad +6.7dB），rwr 保持原样；gradlew build BUILD SUCCESSFUL（32s）；开发日志二、38 已补充响度处理说明；另修复此前同一消息内并行 SearchReplace 同一文件导致目录 58 与二、38 章节被覆盖丢失的竞态问题（已重新插入恢复）
- 【邪恶盖金追击体验二轮调整】用户四点反馈：①**RWR 需要保留距离衰减但衰减要小**——EvilGajinRwrSound 从 Attenuation.NONE + 音量 1.0 改为 **Attenuation.LINEAR + 音量 2.0**（10 格处约 0.75、8 格处约 1.0，远处仍清晰）；②**近身速度 0.12 过慢**——≤10 格档改回中等速度 **0.28**（SPEED_CLOSE 0.12→0.28，保留两档结构：>10 格 0.35 / ≤10 格 0.28）；③**锁定后先播「进攻D点」、音频停止 1s 后再启动 RWR**——实测 lock.ogg 时长 0.897s≈18 tick，设 LOCK_GAP_TICKS=18+20=38 tick；④**追击中周期性停 RWR 插播「进攻D点」**——每 RWR_INTERVAL_TICKS=200 tick（10s）打断一次；实现改为 **RWR 状态机**（PHASE_IDLE/LOCKING/RWR 三态 + rwrPhaseStartTick 计时 + rwrStarted/rwrPlayer 字段），严格"先停再播、先播完再起"保证音频不重叠；同文件重写用 Write 全文件避免此前同一文件多 SearchReplace 并行竞态。gradlew build BUILD SUCCESSFUL（38s）；开发日志二、38 距离变速与 RWR 两节已同步
- 【邪恶盖金RWR音量三轮调整】用户反馈"追击时声音还是太大，衰减应较小但基础音量也不大"。当前 v2.0+LINEAR(16) 近处 1.4~1.5 爆响。尝试 ①覆盖 `getRange()` 返回 32 拉长衰减——**编译失败：getRange() 是 1.20.2+ API，1.20.1 不存在**（ResourceLocation.fromNamespaceAndPath 同款坑）；②改方案：**attenuation=NONE + tick() 内手动衰减**——每 tick 先同步实体位置，再按与玩家的距离计算 `volume = max(0, 1 - 距离/32)`（避开 SoundEngine 默认 16 格陡峭衰减；SoundEngine 每 tick 重读 volume 字段保证即时生效）：32 格平缓衰减、基础音量 1.0、10 格约 0.69、16 格约 0.5、近处不超 1.0 不爆响。gradlew build BUILD SUCCESSFUL（49s）；开发日志二、38 RWR 节已同步
- 【「进攻D点」改客户端无衰减播放】用户反馈"进攻D点音量衰减太大"——根因：服务端 `level.playSound` 广播受 SoundEngine 默认 **16 格固定衰减**限制（16 格外音量归零，1.20.1 无法像 1.20.2+ 那样用 getRange/fixedRange 改衰减范围），锁定/追击时常相隔 16 格以上完全听不见。解决：**目标为玩家时不再服务端广播，改发 S2C 包**——新增 `EvilGajinLockPacket`（ModNetwork id 4，字段仅实体 ID）+ 客户端 `LockCalloutSound`（AbstractTickableSoundInstance：Attenuation.NONE 无衰减 + 音量 1.0 + looping=false 一次性 0.9s + tick 跟随实体位置，实体消失兜底 stop），锁定瞬间与 RWR 周期打断两处调用 `sendLock(player)`；**非玩家目标（村民/铁傀儡等）保持服务端广播**。过程中再现**同一消息内并行 SearchReplace 同文件竞态**：sendLock 方法定义被 import 编辑覆盖丢失（调用点存在但方法没了 → 编译"找不到符号"），单独串行重插后构建通过（35s）。开发日志二、38 语音节已同步（含 16 格衰减限制根因）
- 【语音再增大 + 错误生物新模型】两项：①**除 rwr 外其它语音播放音量 1.0→1.2**（用户反馈"再稍微增大"）：EvilGajin 攻击「干得好/命中」、击杀「摧毁目标」、受击「局势不太妙」、非玩家目标锁定「进攻D点」四处服务端 playSound 音量 1.2，客户端 `LockCalloutSound`（玩家目标锁定/周期插播）音量 1.2（RWR 保持 1.0+手动 32 格衰减不动）；②**替换三个错误生物模型**：素材 `模型\生物\错误生物\新模型` 三个 Blockbench zip（error(1)/null/WARN，各含 json + texture.png）。分析：json 为"每面独立 UV"布局 + **纯红占位贴图**（32/32/16px，uniqueColors=1，透明=镂空）→ 无法 vanilla 展开盒 UV 直渲也不值得重拼。做法：PowerShell ConvertFrom-Json 转换脚本（留存 run/package/newmodels/，生成段 *_java.txt）把 elements 转 Java addBox：**坐标居中**（x 中心 9/8/7、z 中心 8 统一减偏移）、**旋转斜腿独立 part 绕 origin**（error 3 个 origin=(x,6.5,8)≠中心、null 1、warn 2，z 轴 -22.5°，box 局部 from-origin 坐标 + offsetAndRotation(origin,0,0,-0.3927)）；贴图复制为实体贴图 error_mob.png/error_mob_null.png/error_mob_warn.png，LayerDefinition tex 32x32/16x16/32x32（原 64x32 改），三个 Model 类仅换 createBodyLayer（类名/LAYER_LOCATION/渲染器注册不变）。像素字形：ERROR 23 元素宽 46px、NULL 10 元素宽 42px、WARN 17 元素宽 46px，2px 薄板 z 厚 2px。gradlew build BUILD SUCCESSFUL（43s）；开发日志目录 59、二、39 新章节、二、38 响度段补播放音量 1.2 说明
- 【邪恶盖金换 Blockbench JSON 直渲模型】素材 `模型\生物\邪恶盖金\gaijin_t58.zip`（gaijin.json + gaijin.png）：Blockbench 1.9.0，**17 元素**（5 个非零旋转）、texture_size 32x32、每面独立 UV。用户要求"生物无动画，模型直接加载 json 不要转化"（此前 Java 转化过程问题多）——**方案改为运行时直接解析 json 绘制**：新增 `BlockbenchJsonModel`（load 从资源包读 json：elements.from/to 角点、rotation 绕 origin 旋转换算弧度、faces.uv 除以 tex 尺寸归一化；渲染：8 角点旋转后逐面 4 顶点 + 面法线随旋转矩阵变换再经 pose.normal，up/down 面 v 反转）；`EvilGajinRenderer` 由 MobRenderer 改 **EntityRenderer 直渲**（构造加载 json，render 内 -yRot 转向 + entityCutoutNoCull 双面透明镂空）；删除 EvilGajinModel 及 CcRc 中 LayerDefinition 注册；模型 json + 32x32 贴图入 assets。**编译排错关键：1.20.1 官方映射 VertexConsumer 光照/覆盖层方法名是 `uv2(int)`/`overlayCoords(int)`（打包单参），`light(int,int)`/`setLight(int)`/`overlay(int,int)` 均不存在**（经 client_mappings.txt 证实）。gradlew build BUILD SUCCESSFUL（44s）；开发日志目录 60、二、40 新章节（38 模型段补跳转说明）已同步
- 【编辑工具崩溃修复】崩溃报告 crash-2026-09-20_22.32.17-server：`Exception in server tick loop` ← `NoClassDefFoundError: com/cc_rc/item/EditToolItem$1`（EditToolItem.openEditMenu:70）。**根因排查**：EditToolItem.java 自 v0.0.9 未改动、当前 build/classes 与 build/libs 里 `EditToolItem.class` 与 `EditToolItem$1.class` 均存在、22:51 重启的新会话未复现 → 判定为 **dev 环境竞态**：游戏从 `build\classes\java\main`（目录）加载模组类，`EditToolItem$1`（第 70 行匿名 MenuProvider）是**懒加载**（启动时只加载了 EditToolItem.class 本体），而崩溃前恰有 gradle 构建在并发重写 build/classes，首次右键编辑工具时 `$1` 类文件不可读 → NoClassDefFoundError → 整服崩溃（进程命令行 -cp 证实类来自 build/classes 目录）。**修复**：`openEditMenu` 捕获 `Throwable` 防御——记 error 日志 + 向玩家发送中文提示"请重启游戏后重试"，服务端 tick 循环不再被 Error 打断；EditToolItem 新增自有 `LOGGER = LogUtils.getLogger()`（CcRc.LOGGER 为 private 不可跨类访问，编译排错一轮）。**使用注意**：游戏运行时请勿并发执行 gradlew build / 重编译，改完代码需重启游戏生效。gradlew build BUILD SUCCESSFUL（34s）；开发日志二、28 已补防御说明
- 【渲染修复①：错误生物字牌上下颠倒】根因：原版 `LivingEntityRenderer` 渲染前执行 `pose.scale(-1,-1,1)`（绕 Z 180°）+ `translate(0,-1.501,0)`，模型须按"实体语义 y 越大越靠下（脚底 y_px=24）"绘制（GajinModel 类注释即此约定，蜗牛转换时做了翻转）；错误生物新模型转换脚本**遗漏 y 翻转**，几何为 Blockbench 的 Y 向上 → 经 -1,-1,1 后上下颠倒。修复：**不改几何**，在 ErrorMobRenderer.scale() 内再乘 `scale(-s,-s,s)` 抵消原版绕 Z 180° + `translate(0,1.501,0)` 反向补偿原版位移（矩阵推演为纯缩放，字牌按 Blockbench 方向贴地），error/null/warn 三变种共用渲染器一次修复。
- 【渲染修复②：邪恶盖金模型透明不显示】根因：BlockbenchJsonModel 自绘顶点链两处错误——①调用顺序 `uv2(light/UV2)` 先于 `overlayCoords(overlay/UV1)`，而 BufferVertexConsumer 按 VertexFormat 元素游标严格推进，先写 UV2 时游标尚在 UV1 → 元素不匹配**静默跳写**，light 缺失且后续数据错位（javap 反编译证实 areturn 跳过）；②链尾**缺失 `.endVertex()`** → 顶点从未提交（vertexCount 不增长），GPU 无数据可画 → 整只实体"透明"（仅剩阴影）。修复：顺序改 POSITION→COLOR→UV0→UV1(overlay)→UV2(light)→NORMAL + 链尾补 `.endVertex()`。另核实贴图 evil_gajin.png 为 32bppArgb（有镂空透明+不透明主体，正常）。构建待游戏关闭后执行验证；开发日志二、39（颠倒修复）二、40（透明修复）已补说明
- 【渲染修复③：邪恶盖金 大小16倍/UV镜像/位置偏移 三连修】用户实测：模型高度至少 5 格（json 高仅 7.38px）、UV 上下没对上、位置不在碰撞箱；用户已确认模型 json 与贴图本身无问题 → 纯代码问题。**①模型过大根因**：通过 javap 反编译 `ModelPart$Cube.compile` 字节码意外找到关键——原版实体模型渲染在 `vertex()` 前执行 `vertex.pos /16F`（16px=1 格换算），而我的 BlockbenchJsonModel 漏除 16 → 7.38px 渲染成 7.38 格（此前一直误以为顶点单位自动是 1/16 格）。修复：提交顶点坐标 `/16.0F`；**②UV 左右镜像**：FACE_CORNERS 的 east/west 顶点左右顺序颠倒（从面外侧观察方向推导），修正 east={7,6,5,4}、west={2,3,1,0}；**③位置偏移**：模型 x 中心 8px/z 中心 4.1px 未居中 → load() 统计 bbox，render() 内 `translate(-cx/16,-minY/16,-cz/16)` 居中贴地（先平移后 yaw 旋转，绕原点不再甩动）。**顺带验证了错误生物渲染器 scale 补偿方案（-s,-s,s + translate 1.501）数学正确**。gradlew build BUILD SUCCESSFUL（35s）；开发日志二、40 已补三连修说明
- 【渲染修复④：up/down 面 UV 翻转 + 放大 1.5 倍】用户确认大小正常（16 倍修复生效），但 UV 贴图仍对不上、希望放大 1.5 倍。**UV 根因排查**（关键证据链）：反编译原版 `ModelPart$Cube` 构造 + `ModelPart$Polygon` 构造字节码提炼黄金标准——Polygon 顶点 UV 映射为 v[0]→(u1,v0)、v[1]→(u0,v0)、v[2]→(u0,v1)、v[3]→(u1,v1)，联合 Cube 六面顶点顺序推导出**原版实体 Cube 的 up/down 面"贴图上方"朝模型 +Z（南）**（与方块模型 up 面贴图上方=北相反）。此前对 up/down 加的 flipV 把贴图上下翻转 180°，俯视/仰视即见贴图颠倒 → **去掉 flipV，六面统一标准 UV 映射**；侧面经黄金标准逐角点核对全对。**放大 1.5 倍**：EvilGajinRenderer 加 `pose.scale(1.5F)`（作用序：居中→缩放→yaw，中心不漂移）。gradlew build BUILD SUCCESSFUL（34s）；开发日志二、40 已补④
- 【渲染修复⑤：UV 面角点顺序最终修正】用户确认 1.5 倍大小 OK，但贴图仍不对 → 不再猜，改用 **Blockbench 内部面数据结构**（MOPS 插件源码：`CUBE_FACES_TRIANGLE_FANS`=立方体 6 面的 Blockbench 顶点顺序 + `CUBE_UV_INDICES=[[0,1],[2,1],[2,3],[0,3]]`=面 UV 四角索引）逐角点核对出**权威顺序**（左上→右上→右下→左下）：north{2,6,4,0} south{7,3,1,5}（本就正确）、**east{6,7,5,4} west{3,2,0,1}（上一轮"外看推导向"的镜像修改相反，已恢复）**、**up{6,2,3,7} down{5,1,0,4}（up 贴图上方朝 -Z、down 朝 +Z，u 小端均朝 +X；此前 flipV 或标准映射都差 90°/180°）**。**教训**：BLOCKBENCH 每面 UV 方向不能靠"从外看"直觉推导，必须以 Blockbench 引擎导出的角点顺序为准。gradlew build BUILD SUCCESSFUL（36s）；开发日志二、40 补⑤
- 【渲染修复⑥：UV 归一化基准修正（像素颗粒大）】用户：贴图样子有变化但"uv 大小错误、像素点大于正确样子"。**数据铁证**：逐面核对 uv 与模型几何——所有面 uv 区域（x/z 方向）恰好=模型面像素的一半（element[1] north 面 6px↔uv 3、element[0] up 面 5×7↔2.5×3.5、east 面 7↔3.5），即 **gaijin.json 每面 uv 值基于 16 像素网格**（texture_size=32 是其 2 倍）。此前 uv/32 归一化 → 采样区域减半 → 贴图放大 2 倍颗粒大（叠 1.5 倍模型缩放更明显）。修复：UV 归一化统一 `/16.0F`（侧面 v 方向作者未严格按比例，与 Blockbench 预览一致）。**因游戏正在运行未编译，待关闭后 gradlew build 验证**；开发日志二、40 补⑥