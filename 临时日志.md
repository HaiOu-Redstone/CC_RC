# CC_RC 临时日志

> 用途：简略记录每次模组操作，只追加、不修改已有内容。
> 每次打包新版本时，本日志内容将作为该版本的更新说明发布，随后归档到 `临时日志归档/` 并新建空白日志继续记录。

## 2026-09-21

- 【打包 v0.0.11 完成】按「打包发布流程.md」执行：`gradle.properties` mod_version 0.0.10→0.0.11（本周期内容含：海报1 注册/修复/平面图、海报2~16 批量注册、唱片「LEVEL !」、邪恶盖金注册与微调（语音响度归一化、追击体验二轮、RWR 音量三轮、进攻D点客户端无衰减）、错误生物新模型、邪恶盖金 Blockbench JSON 直渲 + 渲染修复①~⑥、编辑工具崩溃防御）；gradlew build BUILD SUCCESSFUL（41s），产物 `build/libs/cc_rc-0.0.11.jar`（156MB）；本周期 21 条临时日志 + v0.0.10 记录已归档至 `临时日志归档/临时日志-v0.0.11.md`；开发日志同步：目录 56~61、二、28（编辑工具防御）/31（LEVEL 唱片补充）/37/38/39/40 章节、三、2 物品 154、三、4 声音 45、三、6 实体 6
- 【发布 v0.0.11 完成】git commit + push 已推送 `main`（SSH over 443，898f0bc..7fea408）；`gh release create v0.0.11` 成功：https://github.com/HaiOu-Redstone/CC_RC/releases/tag/v0.0.11，Release 说明采用归档临时日志内容（21 条精简版），附件 `build/libs/cc_rc-0.0.11.jar`；归档与空白新日志已就绪
- 【服务端启动崩溃修复（合并 PR #2，贡献者 Nuclear-rea）】专用服务器（Mohist，H:\CCNR 环境）latest.log 报 `NoClassDefFoundError: net/minecraft/client/resources/sounds/SoundInstance`，`ModNetwork.init` 注册包 4（EvilGajinLockPacket）时模组构造失败 → 服务器无法启动。**根因**：三个 S2C 包（CrackSoundPacket id1 / EvilGajinRwrPacket id3 / EvilGajinLockPacket id4）的 `handle()` 内联 lambda 直接引用客户端类，其中 `EvilGajinLockPacket` 引用 `LockCalloutSound`，其父链 `AbstractTickableSoundInstance → SoundInstance`（`net.minecraft.client.resources.sounds` 包）为客户端专属类、专用服务器类路径不存在，服务器加载包类沿父链解析即崩。**修复**：三包统一改为**纯数据包**——`handle()` 先 `PENDING` 静态暂存，再 `DistExecutor.unsafeRunWhenOn(Dist.CLIENT, () -> XxxClientHandler::dispatchPending)` 无参方法引用派发（方法引用惰性解析，服务器不加载客户端类）；客户端处理器补 `dispatchPending()`（CrackSoundClientHandler / EvilGajinRwrClientHandler），新建 `EvilGajinLockClientHandler`（播放逻辑自原 LockCalloutSound 内联处移入）。**已 javap 验证**三个包类常量池均无 `net/minecraft/client` 直接引用，仅剩惰性解析的 MethodHandle。**构建**：首次 gradlew build 因 `前置/` 目录缺失（本地依赖未随仓库克隆）失败，已从 gradle 缓存复制 `cc-tweaked-1.20.1-forge-1.120.2.jar` 为 `前置/cc-tweaked-1.120.2.jar` 后 BUILD SUCCESSFUL（38s），产物 `build/libs/cc_rc-0.0.11.jar`；开发日志二、38 已补修复说明。**本地合并**：fetch `pull/2/head:pr2`（0edda01）→ merge（6 个 Java 文件 + DEVELOPMENT_LOG.md 自动合并，仅临时日志.md 冲突已解决：保留 v0.0.11 发布记录 + 采纳本条目）→ gradlew build BUILD SUCCESSFUL 验证 → 推送 main 并关闭 PR。
- 【前置入库 + CC:T 版本兼容判断】用户决定前置入库。①**入库**：`.gitignore` 移除 `前置/` 忽略规则（原注释"从 CurseForge 获取不随仓库上传"已更新为入库说明），`git add 前置/cc-tweaked-1.120.2.jar` 提交推送（commit cc8fd13）——克隆仓库后 flatDir 可直接构建，无需再从外部获取；②**版本核实**：jar 内部 `META-INF/mods.toml` 确认 modId=computercraft、**version=1.120.2**、loaderVersion=[47,48)（Forge 47.x = MC 1.20.1）；对应官方 release tag **`v1.20.1-1.120.2`**（CC-Tweaked/CC-Tweaked，tag 格式 `<MC版本>-<mod版本>`）；③**代码 API 使用面**（21 处 import 全部为稳定公开 API）：`api.peripheral.IPeripheral/IPeripheralProvider`、`api.lua.LuaException/LuaFunction`、`core.computer.ComputerSide`、`api.ForgeComputerCraftAPI`、`shared.computer.core.ServerContext`——多年稳定无破坏性变更；④**兼容性结论**：官方仓库显示 CC:T **持续为 MC 1.20.1 发布版本**（v1.20.1-1.105.0 ~ v1.20.1-1.120.2 共 41 个 tag 全部存在）。官方仓库：https://github.com/CC-Tweaked/CC-Tweaked （releases 页选版本用）。
- 【依赖下限放宽：兼容全部 Forge 1.20.1 的 CC:T】用户拍板"调整，兼容所有 forge1.20.1 版本的 CC 电脑"。查官方仓库确认 1.20.1 分支版本谱系 **1.105.0 → 1.120.2**（41 个 tag），最初版即 **1.105.0**。修改 `mods.toml`：computercraft `versionRange` `[1.120.2,)` → **`[1.105.0,)`**（附中文注释说明兼容范围与稳定 API 依据）。gradlew build BUILD SUCCESSFUL（26s）；日志已更新（本条目）。
