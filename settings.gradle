pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            name = 'MinecraftForge'
            url = 'https://maven.minecraftforge.net/'
        }
    }
}

// Required for auto-downloading JDK toolchains (disabled - using system JDK 21)
// plugins {
//     id 'org.gradle.toolchains.foojay-resolver-convention' version '0.7.0'
// }