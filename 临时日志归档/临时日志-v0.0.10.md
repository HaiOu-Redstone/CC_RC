# CC_RC 临时日志

> 用途：简略记录每次模组操作，只追加、不修改已有内容。
> 每次打包新版本时，本日志内容将作为该版本的更新说明发布，随后归档到 `临时日志归档/` 并新建空白日志继续记录。

## 2026-09-15

- 【注册数据单元方块 data_unit】无方向完整方块，外观暂用原版书架模型/贴图占位（blockstates 与 item model 直接引用 `minecraft:block/bookshelf`，后续换专属模型只需改这两个 JSON）：①方块实体 DataUnitBlockEntity 实现 ITextDisplay（名称字符串，编辑工具右键可改，放置时写入物品自定义名）+ 数据整数列表（初始为空，上限 1024）；线程模型沿用 CC 外设无延迟改造（name/data volatile + 列表写时复制 + dirty + 主线程 tick 节流广播），setValue 支持覆盖/追加（越界补0），NBT 存 Name/Data；②CC 外设 DataUnitPeripheral（type=data_unit，全部无 mainThread 0 tick）：getName/setName 读名称、getList/setList 读写整个列表、getValue/setValue 读写某一位（Lua 索引 1 起）；③注册 ModBlocks DATA_UNIT / ModBlockEntities DATA_UNIT_BE / ModItems DATA_UNIT_ITEM / CcRc 外设 provider / ModCreativeTabs / lang 中英「数据单元/Data Unit」；④gradlew build BUILD SUCCESSFUL（28s）；开发日志已同步（目录 52、二、33、三、1 方块 108、三、2 物品 135/方块物品 74、三、3 方块实体 15）
- 【注册方块探测器 block_detector】完整方块，可向 x±/y±/z± 六方向放置（FACING，放置时面向玩家），外观暂用原版观察者模型/贴图占位（blockstates 六方向映射 `minecraft:block/observer` + 旋转，item model 引用 observer）：①CC 外设 BlockDetectorPeripheral（type=block_detector，方块实体仅作外设承载不存数据）**只读**探测面向方块：getFacing 返回方向、getBlockInfo 返回 {x,y,z,id 注册名,mod 模组来源,name 路径名,isBlockEntity}（目标区块未加载返回 nil）、getBlockEntityData 返回目标方块实体完整 NBT（saveWithId 递归转 Lua 表，含 id/坐标，类似 /data get block，不可修改）；②所有方法标注 mainThread=true（探测需实时读服务端 Level/方块/方块实体/区块加载，Level 非线程安全，1 tick/调用为必然代价，注释已说明）；③注册 ModBlocks BLOCK_DETECTOR（stone 强度2.0）/ ModBlockEntities BLOCK_DETECTOR_BE / ModItems BLOCK_DETECTOR_ITEM / CcRc 外设 provider / ModCreativeTabs / lang 中英「方块探测器/Block Detector」；④gradlew build 两次：首次因方法名 getTarget 与 IPeripheral 接口冲突 + NBT 强转缺失编译失败，改名 getBlockInfo + 强转后 BUILD SUCCESSFUL（26s）；开发日志已同步（目录 53、二、34、三、1 方块 109、三、2 物品 136/方块物品 75、三、3 方块实体 16）
- 【说明书2新增方块探测器章节】InstructionBook2Item.createBook() 目录新增第 5 章「方块探测器」，追加 3 页（共 12→15 页）：概述页（外设类型 block_detector、只读不可修改、**注明每次调用有 1 tick 延迟**、函数清单）+ getFacing/getBlockInfo 页（返回值表 {x,y,z,id,name,mod,isBlockEntity}，未加载返回 nil）+ getBlockEntityData 页（类似 /data get block 只读 NBT，含 Lua 示例）；类注释同步更新为五外设；gradlew build BUILD SUCCESSFUL（24s），开发日志二、21 已同步（页面结构 15 页 + 探测器函数说明）
- 【贴图/模型资源整理】textures/block 与 models/block 顶层散装文件全部归入各方块同名文件夹（彻底无散装）：①多贴图红石类——redstone_receiver（贴图 2 张并入夹并**修正拼写 reciver→receiver**、模型 2 个）、redstone_sender（贴图 6 张、模型 2 个）、relay_bus（贴图 3 张、模型 1 个）、extended_relay（贴图 2 张、模型 1 个）；②贴图已入夹但模型散装的——fridge / potato_crate 模型入夹；③单贴图/单模型也一并入夹——digital_display、digital_knob、digital_plotter、plotter_clock、ccrc_block、sink、canvas_sign（32 个 canvas 标志 blockstates 共享引用）；④**改路径**：所有 JSON 引用同步更新（blockstates 模型引用、模型内 texture 引用、item model parent 引用），用批量精确字符串替换更新 99 个文件（UTF-8 无 BOM 写入保护中文 lang）；⑤校验脚本（临时，已删）确认：全部 blockstates/模型/贴图引用成对存在、无任何无斜杠旧引用残留；⑥gradlew build BUILD SUCCESSFUL（31s）；开发日志已同步（二、35 新增章节）
- 【数据单元/方块探测器换专属贴图】素材取自 `模型\CC电脑类\` 下用户新做的贴图：①**数据单元**——贴图 2 张（`data_unit_top` 顶面 / `data_unit_side` 四周侧面）复制入 `textures/block/data_unit/`；模型复制原版书架结构（`minecraft:block/cube_column` 柱形，`end=top`、`side=side`）至 `models/block/data_unit/data_unit.json`；blockstates 与 item model 由引用 `minecraft:block/bookshelf` 改为 `cc_rc:block/data_unit/data_unit`；②**方块探测器**——贴图 4 张（`block_detector_front` 正面 / `back` 背面 / `side` 侧面 / `top` 顶面，按贴图名字分面）复制入 `textures/block/block_detector/`；模型复制原版观察者结构（16³ 单元素六面模型，front→north、bottom←back→south、top→up/down、side→west/east，particle=front）至 `models/block/block_detector/block_detector.json`；blockstates 六方向映射与 item model 改为 `cc_rc:block/block_detector/block_detector`；③模型文件均从 client.jar 提取原版 bookshelf/observer.json 参照；④grep 验证无 `minecraft:block/observer|bookshelf` 残留、全部新引用成对；gradlew build BUILD SUCCESSFUL（28s）；开发日志二、33/34 与目录 52/53、三、1 方块表、三、2 物品表描述已同步更新
- 【修复扩展继电器识别不到红石中继器输入】现象：中继器对准继电器输出，getInput/getAnalogInput 读不到（红石粉/按钮/拉杆等方向无关信号源正常，仅中继器/比较器等方向相关暴露）：①**两级取证**——反编译原版 DiodeBlock（中继器父类）srg 字节码确认 `Level.getSignal(pos, direction)` 的 direction 是**查询方指向信号源的方向**（中继器 `direction==FACING` 才输出）；反编译 CC-Tweaked 1.120.2 字节码确认 `RedstoneUtil.getRedstoneInput(level, pos.relative(dir), dir)` 与 `RedstoneRelayBlockEntity.getRedstoneOutput = state.getExternalOutput(mapSide(direction))` **均不取反**；②本实现 `refreshInputs`（`dir.getOpposite()`）与 `getRedstoneOutput`（`direction.getOpposite()`）两处取反源于 v0.0.8 修复记录"direction 反向语义"的误导性结论（当时红石粉等方向无关掩盖了错误）；③**修复**：两处去掉 getOpposite（refreshInputs 改 `level.getSignal(pos.relative(dir), dir)`；getRedstoneOutput 改 `toLocalSide(state, direction)`），与 CC 完全一致，并更正两个类 Javadoc；④gradlew build BUILD SUCCESSFUL（32s）；开发日志二、29 追加 v0.0.9 修复记录（声明覆盖 v0.0.8"反向语义"过时结论）；遗留 run/package/repeater_srg.txt 诊断输出
- 【输出方向修正（v0.0.9-2）】用户实测：输入修复（refreshInputs 用 dir 不取反）**正确** ✓；但同轮把 getRedstoneOutput 改为不取反后 **输出前后左右上下全反** ✗。**最终结论：输入与输出的查询方向语义恰好对称**——输入（本方块查邻居）传"本方块→信号源"方向 `dir` 不取反；输出（红石线/邻居查本方块）传入的 `direction` 是"查询方→本方块"方向，与本方块实际输出方向相反，**必须取反**。已恢复 `getRedstoneOutput = toLocalSide(getBlockState(), direction.getOpposite())`（与 v0.0.9 修复前一致，即本次最终只改 refreshInputs 一处）；两个类 Javadoc 同步更新为"输入输出对称"说明；gradlew build BUILD SUCCESSFUL（32s）；开发日志二、29 修复记录已拆分补正（v0.0.9-1 输入 / v0.0.9-2 输出）
- 【修复FAAS服务器循环音效炸响】现象：玩家靠近开播时"炸响"，远离一定距离后仍有**连续不断炸响**（服务端测试）。**两级根因**：①**音频文件被拼接拉长**——素材源 `模型\FAAS\server_noise.ogg` 仅 **8 秒**，而运行时 `sounds/server_noise.ogg` 却是 **20 秒**（历史处理把 8s 素材拼接/拉长成 20s、stereo 192k）——循环音效（looping）每到内部拼接点（8s/16s 交界）和 20s 循环点波形跳变 → 一阵阵爆音=“连续不断炸响”；已用素材源重新转码 **8 秒 / mono 44.1k / vorbis 160kbps**（`-ac 1 -ar 44100 -c:a libvorbis -b:a 160k`），循环单遍即素材原子状态；②**开播满音量**——`ServerFaasSoundHandler.FaasLoopingSound` 构造器 `volume` 固定 `BASE_VOLUME=1.0`，玩家在 16 格扫描边缘进入也满音量开播、且 16~20 格清理阈值边界反复进出会反复重建音效 → 每次进入/重建“炸响”一声；已改**构造时按玩家与方块实际距离初始化音量**（`clamp(1-dist/16)`，边缘≈0，再由 updateVolume 平滑收敛）；gradlew build BUILD SUCCESSFUL（38s）；开发日志二、19 追加修复记录
- 【修复FAAS循环音效"衔接处降低又升高"（终极）】用户反馈上轮 dynaudnorm 方案后仍"比较明显"。**最终根因（逐段 100ms 测绘）**：素材源 `模型\FAAS\server_noise.ogg` **开头 0~0.4s fade-in**（100ms RMS：-25.9/-17.5/-14.1/-13.3）、**结尾 7.8~8.0s fade-out**（7.8s -17.7 / 7.9s -26.1）——循环从尾 -11.5dB 跳到头 -18dB 再爬升，每 8 秒一次"降低又升高"。**最终修复（三层）**：①**音频**：ffmpeg 裁剪纯净中段 `-ss 0.5 -t 7.2`（避开 fade-in 0.5s 与 fade-out 0.3s），`volume=-1.5dB` 留余量、mono 160kbps → **首尾 100ms RMS -12.79 vs -12.43dB（差 0.36dB）无缝**，0.5s 段 -11.9~-13.7dB 无 fade 低谷，峰值 -0.27dB；②**代码（反编译实证）**：SoundEngine `m_120326_`(tick) 每 tick 调 `m_120324_` 重算音量（读实例 volume 字段）→ `updateVolume` 从 `lerp(0.2F)` 平滑改为**绝对设置** `volume=clamp(1-dist/16)*BASE_VOLUME`，静止时音量恒定、移动严格跟随；③**弃用记录**：dynaudnorm 帧增益（噪声泵动+首尾补偿不足）、acrossfade（FFmpeg 4.2 崩溃）、PCM 交叉混合（fade 区能量凹）均弃用；gradlew build BUILD SUCCESSFUL（28s）；开发日志二、19 修正为最终方案
- 【注册反应堆启动音乐（两个声音）】素材 `模型\音频素材\反应堆正常启动音乐.ogg`（3:51、stereo 128k）转 mono 44.1k vorbis 160k 为两个声音：①`reactor_start`（56s 版）——截取前 56s 且末尾 4s 淡出（`-t 56 -af afade=t=out:st=52:d=4`，淡出算在 56s 内，尾部 55~55.5s RMS -28dB 淡出生效），时长 55.99s；②`reactor_start_full`（完整版）——全曲 3:51.06；两者存入 `sounds/music/`、sounds.json 均 `stream: true`（长音乐流式），ModSounds 注册 `REACTOR_START`/`REACTOR_START_FULL`，lang 中英 subtitle「反应堆启动 / Reactor Startup」「反应堆启动音乐（完整版）/ Reactor Startup (Full)」；**仅指令播放无唱片**（`/playsound cc_rc:reactor_start @p`、`/playsound cc_rc:reactor_start_full @p`）；gradlew build BUILD SUCCESSFUL（28s）；开发日志三、4 声音 36→38 已更新
- 【控制面板文字样式（染料染色 + 编辑工具格式按钮）】用户需求：面板文字可染色（指令染色命名放置后颜色保留已实测，本轮补修改手段）、编辑工具 GUI 加字体按钮。**实现**：①**染料染色**——`ConsolePanelBlockEntity.applyDyeColor(DyeColor)` 仅覆写文字颜色（`Style.withColor(TextColor.fromRgb)`，保留原文与其他格式，文字可为空先染色后写字）；`ConsolePanelBlock` 新增静态 `isDyeTarget`/`applyDye`/`handleDyeInteraction`（客户端预测返回成功、服务端染字+消耗 1 染料+`DYE_USE` 音效）；接入点：基类覆写 `use`（无交互子类 panel/large/meter/point_lamp/plotter 生效）＋按钮类 4 个（console_button/safe_button/nuke_button/password_inputer）与拉杆类 2 个（console_lever 覆写 use 调 super、console_lever_6/7）各自 `use()` 开头加染分支（染料优先不影响原交互）；②**编辑工具 GUI**（EditTextScreen 176x166 内向下方扩展）——完成按钮下新增 B/I/U/S 四格式切换按钮（激活白字粗体/未激活灰字）+ 16 染料色块按钮（8x2，选中白框），打开时从 `currentText.getStyle()` 读当前样式作初值，提交携带完整样式（color=-1=默认白）；③**EditTextPacket** 扩展 color(int)/bold/italic/underline/strikethrough 字段，服务端 `Component.literal(text).withStyle(style)` 重建写入（空文本仍清空）；**编译实证**：MC1.20.1 Block **无 useItemOn 方法**（按子类惯例改覆写 use 解决），`ResourceLocation.fromNamespaceAndPath` 为 1.20.3+ API 弃用（沿用项目 `new ResourceLocation` 写法）；gradlew build BUILD SUCCESSFUL（28s/26s）；开发日志新增二、36 与目录 55
- 【控制面板文字样式微调】用户反馈两处：①染色改为**不消耗染料**（`applyDye` 删 `stack.shrink(1)`，理由：编辑工具 GUI 已能直接改颜色，染料仅作快捷手段）；②格式按钮未激活态由灰色 `0xA0A0A0` 改**白色** `0xFFFFFF`（仅以粗细区分，暗背景下可读性更好，`refreshFormatButton` 简化为 `withBold(active)`）；类注释/javadoc 同步更新；gradlew build BUILD SUCCESSFUL（29s）；开发日志二、36 与目录 55 描述已同步修正