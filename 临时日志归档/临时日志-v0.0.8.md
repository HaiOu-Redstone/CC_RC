# CC_RC 临时日志（v0.0.7 周期归档）

> 用途：简略记录每次模组操作，只追加、不修改已有内容。
> 每次打包新版本时，本日志内容将作为该版本的更新说明发布，随后归档到 `临时日志归档/` 并新建空白日志继续记录。

## 2026-09-07

- 上传模组至 GitHub（https://github.com/HaiOu-Redstone/CC_RC），完成首次提交 f33d9af
- 发布 v0.0.7 Release（cc_rc-0.0.7.jar，104.2MB）
- 建立临时日志制度并写入项目规则
- 重构项目规则：拆出「打包发布流程.md」「注册物品规则.md」两个按需读取文档，主规则精简为 6 条常态要点

## 2026-09-08

- 修复 FAAS 服务器声音：显式归类 SoundSource.BLOCKS（可由「音效/方块」滑块调节音量），确认 LINEAR 距离衰减，停止距离由 24 格收敛为 20 格
- 替换 FAAS 模型：使用「模型/FAAS/FAAS新」三组 Blockbench 新模型重新转换为 Java block 模型（faas_1/2/3，55/52/49 元素）并更新贴图
- 构建通过（gradlew build，BUILD SUCCESSFUL）
- 【再次修复】FAAS 声音衰减：实测 SoundEngine 对循环音效不随玩家移动刷新音量（LINEAR 无效），改为 relative=true + 关闭引擎衰减，由客户端每 tick 手动按距离计算音量（16 格内 100%→0%，带 lerp 平滑），衰减必定生效
- 注册控制台按钮 4、5：复用 ConsoleButtonBlock 类，模型取自「模型/特殊控制台/A、C」（核弹面板按钮 on/off 两态），转换时做了 UV 归一化（像素÷texture_size×16）与顶/底面翻转；新增方块/物品/方块实体/创造标签/lang/blockstate，构建通过（BUILD SUCCESSFUL）
- 注册密码输入器 password_inputer（继承 ConsolePanelBlock：放置/碰撞箱同控制面板，on/off 两态，破解成功向后方强充能 15，1 秒后自动关；模型「模型/特殊控制台/密码输入器」）+ 破解器 password_cracker（不可堆叠，手持右键密码输入器开始破解，默认 20 秒可配置 crack_ticks，离开 5 格/切快捷栏即中断重置，破解中 action bar 显示剩余秒数；模型「模型/特殊控制台/破解卡」）；新增 PasswordInputerBlock/PasswordCrackManager/PasswordCrackerItem 三个类，构建通过（BUILD SUCCESSFUL），开发日志已同步（索引 42、章节 24、注册表 102/115）
- 【修复】按钮 4/5 贴图错乱：源模型（现实标注 Bedrock）的 UV 实为 Java 0-16 归一化坐标，与贴图内容区精确吻合（128 贴图内容 0~70px ↔ UV 8.875×128/16≈71px），之前误按 texture_size(64) 缩放导致贴图错位；已统一改为"UV 直接沿用 + 顶/底面翻转"，并顺带重建同批全部 9 个模型（按钮 4/5×off/on、密码输入器×off/on、faas_1~3），构建通过（BUILD SUCCESSFUL）
- 【破解器修复】1) 破解完成崩溃：根因是迭代 SESSIONS 时 SESSIONS.put() 改结构导致 ConcurrentModificationException，改用 entry.setValue() 修复；2) 提示改 action bar（屏幕中间），倒计时从 19 开始（前 1 秒显示"开始破解…"）；3) 破解器改为 3D 物品模型（破解卡.json 转换，26 元素+display）；4) 新增破解音效 password_crack（破解.ogg），服务端经 CrackSoundPacket(S2C) 通知客户端循环播放/停止，中断即停；构建通过（BUILD SUCCESSFUL）
- 【破解器二次修复】仍崩溃（onServerTick:131 ConcurrentModificationException）：根因是破解成功 powerOn→setBlock 触发 onRemove 无条件调用 onBlockRemoved，与 tick 迭代交错修改 SESSIONS；双重修复：onRemove 仅在方块真正被破坏时才清理会话 + SESSIONS 改 ConcurrentHashMap；构建通过（BUILD SUCCESSFUL）
- 【紫黑块修复】密码输入器模型被 Blockbench 新版导出直接覆盖成 Bedrock 1.21.11 格式（rotation 用 x/y/z 欧拉角），Java 加载器报 Missing axis → 紫黑块；已以用户新版模型为源重新转换为 Java 格式（欧拉角→axis/angle，去除 format_version，UV 沿用+顶底面翻转，34 元素全单轴），保留用户全部改动；构建通过（BUILD SUCCESSFUL）
- 【紫黑块二次修复】转换后日志改报 Invalid rotation 90（Java 元素级旋转仅允许 -45~45°）：用户 1.21 新版模型 34 元素全带 x 轴 90/67.5/112.5° 且 8 个非轴对齐不可表示；改用素材源 code_reader.json（19:40 Bedrock 1.9.0 合法版）重新转换 + 用户 19:48 新贴图（MD5 一致），验证坐标 0~16、0 非法角度，jar 与 build/resources 均更新；构建通过（BUILD SUCCESSFUL）
- 【注册核弹按钮+钥匙】nuke_button（继承 ConsolePanelBlock，放置同控制面板；6 状态 STATE 1~6 输出 0/5/7/10/10/15，72 变体 blockstate；钥匙推进状态并消耗，状态4/5 shift 互切，状态5 右键发射 60tick 自动复位；模型"模型/特殊控制台/B"6 文件转换，保持原尺寸仅 z+2 对齐）+ key_1/key_2（stacksTo(1)，贴图 key_1/key_2.png）；已入创造标签与中英文语言；构建通过（BUILD SUCCESSFUL）
- 【核弹按钮渲染修复】B 组贴图透明区域渲染成黑色：默认 solid 渲染类型会把 alpha=0 像素写黑；在 CcRc ClientModEvents.onClientSetup 中为 nuke_button 设置 ItemBlockRenderTypes.setRenderLayer(RenderType.cutout())（alpha test 剔除透明）；构建通过（BUILD SUCCESSFUL）
- 【注册钥匙柜+钥匙分发控制器】key_cabinet（水平四向放置，模型"钥匙柜.json"转换，多功能工具右键记录坐标朝向）+ key_distributor（六面 key_sender 贴图 cube_all，新 BE key_distributor_be 存 KeyCabinetRecord 列表 NBT 持久化）；多功能工具右键控制器录入记录；红石 ≤5 校验删除失效记录、>5 随机取两条不同记录生成钥匙1/2 掉落物（setGlowingTag+ lifespan=MAX_VALUE 不消失；1.20.1 无 setLifespan 改用公开字段）；/ccrc keycabinet list/add/remove（须站控制器上方）；构建通过（BUILD SUCCESSFUL）
- 【钥匙分发控制器修复】① 一次红石脉冲生成多把钥匙：neighborChanged 被多次触发 → 改为方块 tick 心跳 + BE lastSignal 上升沿检测，脉冲只分发一次（只出钥匙1+钥匙2 两把），持续 >5 不再重复分发；② 钥匙柜碰撞箱改为贴墙长方体（宽12 x2~14 / 高14 y0~14 / 厚7 z9~16，随 FACING 四朝向旋转 getShape）；③ 掉落物生成位置修正为柜门方向（FACING.getOpposite()，模型柜门朝 FACING 反方向）；构建通过（BUILD SUCCESSFUL）
- 【钥匙柜再修复】用户更新了模型（默认贴 z0~8 侧）；① 放置朝向改回玩家反方向（FACING=opposite）使对墙放置柜体贴墙、柜门朝玩家；② 碰撞箱四朝向重算匹配新模型（south 基准 z0~7，north z9~16，east/west x 相应）；③ 贴图 61% 透明 → 为 key_cabinet 与 key_distributor 设置 RenderType.cutout()（solid 渲染透明处会变黑导致"颜色很暗"）；清理模型 format_version 残留；构建通过（BUILD SUCCESSFUL）
- 【钥匙掉落位置调整】分发钥匙1/2 生成位置由"柜门前一格中心"改为"钥匙柜所在格内中心向柜门方向（FACING 反方向）偏移 0.3 格"（用户要求：生成在钥匙柜这一格的中心远离钥匙柜的位置）；修复一次注释编辑失误；构建通过（BUILD SUCCESSFUL）
- 【钥匙静止生成】spawnKey 掉落物增加 setDeltaMovement(0,0,0)（ItemEntity 构造器默认赋随机散布速度，需清零使生成时静止）；构建通过（BUILD SUCCESSFUL）
- 【钥匙生成方向修正】钥匙生成偏移方向由 FACING.getOpposite() 改为 FACING 本身：用户新钥匙柜模型（贴 z0~8 侧）柜门朝 FACING 方向（旧模型柜门朝 FACING 反方向，换模型后需跟随改）；构建通过（BUILD SUCCESSFUL）
- 打包 v0.0.8：版本号 0.0.7→0.0.8，gradlew build 成功（cc_rc-0.0.8.jar，109,722,111 字节），提交 ea7e099 推送 main，gh release create v0.0.8 发布成功