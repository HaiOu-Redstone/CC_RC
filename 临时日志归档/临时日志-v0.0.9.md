# CC_RC 临时日志

> 用途：简略记录每次模组操作，只追加、不修改已有内容。
> 每次打包新版本时，本日志内容将作为该版本的更新说明发布，随后归档到 `临时日志归档/` 并新建空白日志继续记录。

## 2026-09-14

- 【扩展红石继电器传导修复】用户反馈继电器/总线像玻璃一样不传导红石但未生效。排查：①测试 jar（23:40）里 isRedstoneConductor 未覆写（完整方块默认 true=传导）；②工作区今日 9:46 已尝试添加 @Override isRedstoneConductor 但**编译失败**（1.20.1 中该方法定义在 BlockBehaviour.Properties 的 StatePredicate，Block 层无可覆写实例方法），故 jar 一直未更新；③经 Forge 1.20.1 教程/论坛确认正确写法。修复：ExtendedRelayBlock/ExtendedRelayBusBlock 构造器改用 `super(properties.isRedstoneConductor((state, level, pos) -> false))`（配 canConnectRedstone=false），修正总线过时注释（canConnectRedstone=true→false）；gradlew build BUILD SUCCESSFUL（12:06，110,143,000 B），开发日志二、29 追加第 5 条修复记录
- 【说明书内容更新】①说明书1：控制面板类新增「编辑工具」条目（共 8 页：封面→目录→控制面板类→编辑工具续→圆盘记录仪→断路器→刷卡机→红石收发），描述主手右键可显示名称方块打开文字编辑界面、副手放置自动打开；②说明书2：新增「扩展红石继电器/总线」3 页（共 12 页），记录外设 redstone_relay_bus 全部 7 个 Lua 函数签名（isRelay/setOutput/getOutput/setAnalogOutput/getAnalogOutput/getInput/getAnalogInput）、distance 语义（紧贴=1，最大 relay_bus.max_distance 默认16）与 side 基准（继电器自身朝向）；构建 BUILD SUCCESSFUL，开发日志已同步（目录 38/39、二、20/21、三、2 说明书条目）
- 【注册盖金蜗牛 gajin 与金鹰 golden_eagle】素材「模型/生物/蜗牛」（gaijin.bbmodel 含内嵌 64×64 贴图 + 金鹰.png + 蜗牛音效.mp4）：
  - 实体 gajin（GajinSnail）：被动动物（MobCategory.CREATURE，0.5×0.5），AI 参考原版猪但无乘骑（Float/Panic 2.0/Breed/Tempt 金鹰 1.25/FollowParent/游走/注视/环视），右键 mobInteract 播放 gajin 音效（SoundSource.NEUTRAL），手持金鹰进入喂食/繁殖，isFood=金鹰，getBreedOffspring 同型，属性对齐猪（10 血/0.25 速），不自然生成；模型 GajinModel（4 box：身体4×2×12/外壳3×8×8/左右触角1×3×1，贴图由 bbmodel 自由 UV 经 PowerShell 重排为 Java 实体 Mojang 布局 64×64 存 textures/entity/gajin.png，无腿部动画仅 yaw 转身）；渲染器 GajinRenderer shadow 0.3
  - 物品 golden_eagle 金鹰（堆叠 64，贴图金鹰.png→textures/item/golden_eagle.png，蜗牛食物）；刷怪蛋 gajin_spawn_egg（ForgeSpawnEggItem 惰性解析，主色金褐 0xD8B24A/次色深褐 0x5A3A1E）
  - 音效 gajin：蜗牛音效.mp4 经 VLC 转码 vorbis → sounds/gajin.ogg（270KB），sounds.json + subtitle（中英 lang）
  - 注册：ModEntities/CcRc（属性/渲染器/模型层）/ModCreativeTabs（金鹰+刷怪蛋）/lang 中英；gradlew build BUILD SUCCESSFUL；开发日志已同步（目录 48、二、30、三、2 特殊物品 29 个/物品总数 128、三、4 声音 28、三、6 实体 5）

- 【修复盖金蜗牛】用户反馈三问题：①刷怪蛋贴图误用 bbmodel 内嵌实体贴图，改为素材文件夹 gaijin.png（16×16 刷怪蛋贴图）；②模型上下颠倒+浮空——根因 java_block（Y 向上）与 Java 实体模型（Y 向下，y=24 贴地）坐标系差异，坐标整体翻转（body y21..24 / shell y15..21 / 触角 y17..21）；③模型太小放大至约半方块（body 8×3×12、shell 6×6×8、触角 2×4×1，texOffs 重排为 shell(0,0)/tentR(28,0)/tentL(34,0)/body(0,16)）；gradlew build BUILD SUCCESSFUL，开发日志二、30 已追加修复记录
- 【CC 外设延迟评估（只读分析，未改代码）】逐外设 @LuaFunction(mainThread=true) 分布：数码显示器 setStatus/getStatus 均标注（读写都 1 tick，getter 也标是本组唯一）；数字调节器 setValue 标注（写 1 tick）/getValue 无标注（读 0 tick）；圆盘记录仪 push/setValue 标注 /getValue/getList 无标注；继电器总线 7 方法全标注（且 getInput 读的是 tick 轮询的 inputs 上 tick 缓存，输入最坏 2 tick）。改写为无延迟的风险：①写路径 sync() 调 setChanged()+level.sendBlockUpdated（主线程 API），直接删标注会线程不安全/崩溃；②BE 字段（status/value/data）非 volatile，跨线程读写可见性无保证；③去标注后 Lua 高频写不再被主线程限流，会触发广播风暴卡顿，需原版 RedstoneState 式双缓存+每 tick 合并广播；④总线 findRelay 依赖访问 Level，需线程安全缓存+主线程校验，成本最高。结论：1 tick 符合 CC 生态惯例，维持现状
- 【连续读取多继电器延迟分析】CC 的 MainThreadScheduler.tick() 在单次主线程 tick 的时间片内批量消费同一电脑连续排队的 mainThread 任务（执行任务→resume Lua→Lua 再次调用继续排队→同 tick 继续消费），故 Lua 连续读取数十个继电器（中间无 os.sleep/os.pullEvent/coroutine.yield 等挂起点）只跨 1 个主线程 tick 边界=仅 1 tick 延迟，不会随读取个数累积成几十 tick/几秒；仅当调用间夹挂起调用，或任务量超过单 tick 时间片（数十个 findRelay 为微秒级不可能）时才会跨多个 tick
- 【外设延迟通用性+依赖链分析】①该「同 tick 批量消费」是 CC 全局 MainThreadScheduler 机制，对 4 个外设（digital_display/digital_knob/digital_plotter/redstone_relay_bus）及跨外设混合连续调用一律成立，唯一必要条件=同一台电脑 Lua 执行流中无显式挂起点；②读→算→写依赖链也仅 1 tick：getInput 返回值在 Lua 侧同步参与运算，setOutput 排队时参数已固化，链上全部 mainThread 任务由同一 tick 批量消化；③修正上一轮「写后立即读可能读到旧值」说法：mainThread 写令协程挂起、主线程写完才 resume，resume 后 getter 在同一个主线程执行流内同步读，必然读到新值（同线程顺序执行天然可见）；真正旧值风险仅存在于跨电脑并发写+同时读的极端场景，本模组无其他线程写字段，实际风险接近零
- 【总线输入轮询缓存上界】总线 getInput 读 inputs[]（tick 轮询上 tick 缓存），「信号变化→读到新值」需先等一次轮询（≤1 tick）+ mainThread 批次（1 tick），上界 2 tick，不随次数累积
- 【循环结构延迟分析】for/while/ipairs 等循环迭代间无显式挂起点，所有迭代内 mainThread 任务仍由同一主线程 tick 嵌套批量消化，「循环遍历读取一排继电器(1~64个)写数组」总延迟仍 1 tick，不因循环次数累加（数组写入为 Lua 侧内存操作不参与）。会拆分延迟的仅三者：①迭代体内有 os.sleep/os.pullEvent/coroutine.yield 显式挂起点；②单 tick 时间内 mainThread 任务总耗时超过 MainThreadScheduler 时间片预算（约毫秒级），剩余任务顺延下 tick——需数千次调用或单任务本身昂贵才会触发，数十次 findRelay 为微秒级，安全余量极大；③任务本身执行沉重的世界副作用
- 【★纠正 mainThread 延迟结论（用户实测 print(isRelay(i)) 每 tick 输出一个触发查证）】依据 CC-Tweaked 1.120.2 deobf jar 内类清单 + jsdelivr 抓取 projects/core 源码证实：①Lua 运行于 ComputerThread 的独立低优先级 WorkerThread（多线程池），**非主线程**；②mainThread 方法经 Computer#queueMainThread 排队到本机 MainThreadExecutor，主线程 MainThread.tick() 的 MainThreadExecutor.execute() **每次只 poll 一个任务**执行；③任务执行后 resume Lua 需经 ComputerThread 重新排队（异步跨线程），新任务只能在 WorkerThread resume Lua 后产生——单台电脑连续 mainThread 调用**无法**在同一主线程 tick 内串联完成。**正确结论：每个 mainThread 调用 ≈ 1 tick（约50ms），连续 N 个调用 = N tick，不批量不合并**；读→算→写依赖链 = 读1+写1 = 2 tick；循环 N 次 ≈ 调用总数 tick；④print/term.write 为 TermMethods.write（@LuaFunction 无 mainThread，synchronized(terminal) 线程安全），在 WorkerThread 同步写终端缓冲、主线程下一 tick 发客户端显示——与实测「每 tick 输出一个」吻合。**此前记录的同 tick 批量消费/循环不累加结论全部作废，以本条为准**
- 【总线 getInput 上界修正】input 轮询缓存 ≤1 tick + mainThread 每调用 1 tick → 信号变化到读到新值上界 2 tick（每调用各 1 tick 不合并）
- 【CC 外设无延迟改造——方案定案（纯评估记录，暂不改代码）】用户要求 4 个已注册 CC 外设全部去除「每 mainThread 调用 1 tick」延迟，选定风险最小方案：
  - 统一架构：Lua 线程（WorkerThread）同步读写线程安全状态（0 tick）；主线程每 tick 合并应用与广播（dirty 节流，sendBlockUpdated 每 tick ≤1 包）。**已否决 NBT 载体方案**（CompoundTag 非线程安全 + inputs 落盘污染 + 镜像/继电器/世界三态一致，仅换得跨模组解耦，收益不抵风险）
  - 第 1 类（低风险，先实施）：digital_display / digital_knob / digital_plotter——状态字段 volatile 化（显示器 Component 引用、调节器 int、记录仪 volatile int[] 引用+写时复制不可变快照）+ dirty 标记 + 新引入 BE 主线程 tick（Block.getTicker + createTickerHelper，当前三者均无 tick）+ 节流广播；外设方法全部去掉 mainThread=true（getter/setter 同步 0 tick）
  - 第 2 类（高风险，后实施）：redstone_relay_bus——复刻 CC RedstoneState：继电器状态对象（internal 双缓存 + ReentrantLock 多电脑并发写保护）；总线 BE 主线程维护「distance→状态对象」表并校验（防继电器拆除/重放后缓存失效）；Lua 线程经外设**只读写状态对象、零 Level 访问**（杜绝 findRelay 式崩溃）；主线程 tick 合并应用输出（updateNeighborsAt 每 tick ≤1 次）+ neighborChanged 事件驱动刷新 inputs（废弃 tick 轮询）；**顺带修复现存 Inputs 落盘污染**（红石输入是瞬态信号不应持久化，重启会读到过期快照）
  - 实施顺序（风险升序、每步构建验证）：digital_knob → digital_display → digital_plotter → redstone_relay_bus
  - 硬性约束：①外设方法严禁在 Lua 线程访问 Level；②dirty 写序=字段写先、dirty=true 后（volatile happens-before）；③广播每 tick ≤1 次；④记录仪写操作永不修改共享数组（只换引用）；⑤每阶段 gradlew build + 游戏内实测（如 for i=1,1000 连续读瞬时完成、print(isRelay(i)) 整批输出）
- 【GitHub issue #1 唱片音乐】处理 HaiOu-Redstone/CC_RC issue #1（用户 Romarku 上传 6 首歌曲+封面）：①curl 下载 6 个 mp3 + 6 个 16×16 png 至「模型/唱片/」（bit/broken_boy/panic_track/resonance/roller_mobster/sabotage）；②VLC 转码 mono OGG（44.1kHz/vorb/160kbps）至 sounds/music/，验证 ch=1，实测 ticks=3187/4847/3075/4254/4286/4540；③注册 ModSounds 6 个 SoundEvent + ModItems 6 个 RecordItem（comparator 复用 3/4/7/8/9/10）+ sounds.json（stream）+ 6 个 item 模型 + lang 中英 + music_discs 标签 + 创造栏；④gradlew build BUILD SUCCESSFUL（34s）；⑤开发日志已同步（目录 49、二、31、三、2 物品总数 134/音乐唱片 30、三、4 声音 34）
- 【盖金蜗牛受击能力】覆写 GajinSnail.hurt：实际受伤（super.hurt 返回 true）且服务端时施加「抗性提升 II」+「生命回复 II」（amplifier 1，30×20=600 tick，仅服务端）；gradlew build BUILD SUCCESSFUL（26s）；开发日志二、30 已追加
- 【CC 外设无延迟改造·实施】按风险最小方案改完全部 4 个外设：①knob/display/plotter：字段 volatile（plotter 写时复制快照）+ 写仅置 dirty + Block.getTicker 注册 BE tick 节流广播（每 tick ≤1 包）+ 外设方法全去 mainThread（0 tick）；②relay_bus：新增 RelayState（ReentrantLock 双缓存 internal/applied/inputs），总线 BE 主线程每 tick 重建 distance→RelayState 不可变缓存（volatile 替换），Lua 零 Level 访问，输出主线程 tick 合并应用（变化面掩码→updateNeighborsAt），输入 neighborChanged 事件+tick 刷新，并修复 Inputs 落盘（瞬态信号不再持久化）；③gradlew build BUILD SUCCESSFUL（25s）；④开发日志已同步（目录 50、二、32）；待游戏内实测：for i=1,16 print(isRelay(i)) 应整批瞬时输出、setOutput 后红石 ≤1tick 生效
- 【start_client.bat 启动故障三层修复】①双击报「'-'/'0.20+8'/'Minecraft'/乱码 不是内部或外部命令」：根因=脚本曾含 UTF-8 无 BOM 中文注释，cmd 双击按 GBK 解析成乱码命令 → 脚本改纯 ASCII；②banner 后窗口几秒自动消失且 run\start_client.log 未被写入：根因=if ( ) 括号块内 echo 文本含左括号 (（"Two clients cannot share the same run/ folder."），cmd 按括号配对计数解析时把后续行（含 gradlew 启动行）全吞进 if 块 → 改 goto 标签结构、echo 文案避开括号、路径全部 %~dp0 绝对化；③用户双击偶发 runClient 失败 exit 268435466（=0x1000000A，java.exe 启动 2s 内主动退出，无 hs_err_pid 崩溃转储、无 Windows 事件日志、gradle log 无 JVM 输出），本环境以真实双击语义（ShellExecute 独立控制台）连续 6 次启动成功无法复现，判断为用户侧环境偶发：已加单实例保护（start_client_guard.ps1 检测 BootstrapLauncher 进程，防旧实例未退出时撞 run 目录文件锁）+ runClient 失败自动重试最多 3 次（间隔 8s）+ 完整输出落盘 run\start_client.log（控制台仅显示 banner 属正常设计）；用户最终确认可正常启动
- 【总线无法识别继电器修复】无延迟改造后用户反馈：外设瞬间执行生效，但总线 isRelay 无法识别扩展红石接口。根因：总线 BE 的「distance→RelayState」缓存刷新依赖主线程 tick，但 ExtendedRelayBusBlock 漏覆写 getTicker（改造前外设 mainThread 实时 findRelay 查 Level 不依赖缓存；改造后读缓存但缓存从不重建 → relayCache 恒空）。修复：补 getTicker（手写 lambda，与 knob/display/plotter 同款）驱动 ExtendedRelayBusBlockEntity.tick 每 tick 重建缓存 + rebuildCache 加 level.isClientSide 过滤；继电器侧无此问题（ExtendedRelayBlock.tick 方块自调度持续）。gradlew build BUILD SUCCESSFUL（34s），开发日志二、32 已追加
- 【两处小修复】①红石线连接继电器：ExtendedRelayBlock.canConnectRedstone false→true（isSignalSource 原本 true；isRedstoneConductor=false 保持非导体语义，红石线不从继电器穿透传导）；②蜗牛音效"1.5 遍"：根因=源素材蜗牛音效.mp4 时长 16.7s 本身含「一遍(~11s)+半遍(~5.5s)」重复未剪。本沙箱 VLC 播放/转码被拦截（help 可输出但播放静默失败），改经 npm 镜像 registry.npmmirror.com 下载便携 ffmpeg（@ffmpeg-installer/win32-x64，64MB，留存 run/package/ffmpeg.exe 供后续音频处理）→ ffmpeg 裁剪重转 gajin.ogg=11.00s/mono/44100Hz/vorbis/160kbps（194KB），另在 GajinSnail.mobInteract 加 0.5s 播放防抖（lastSoundGameTime）；gradlew build BUILD SUCCESSFUL（29s），开发日志二、30/32 已同步
- 【修复蜗牛右键音效完全无声】用户反馈重转码后右键无任何音效、/playsound 也无声。排查：①gajin.ogg 本身音量正常（ffmpeg volumedetect：mean -10.5dB / max -3.5dB）、可正常解码、src 与 build/resources 两份 MD5 一致——文件无问题（用户确认此前 /playsound 无声为游戏端资源未刷新）；②真正根因=右键防抖 long 整数溢出：lastSoundGameTime 初始为 Long.MIN_VALUE，`now - lastSoundGameTime` 在 Java 中溢出成约 -9.2e18 的负数，`>= 10` 恒为 false → 首次及后续右键音效**永远不播放**。修复：比较前先排除哨兵值（`lastSoundGameTime == Long.MIN_VALUE || now - lastSoundGameTime >= 10`），注释同步说明；gradlew build BUILD SUCCESSFUL（34s），开发日志二、30 已追加
- 【修复三个错误生物模型贴图文字错位】用户反馈 error/null/warn 三个模型的贴图文字位置不对（y 方向正确），贴图上文字左下角像素 x=2/15/28/41/54、y=18（null/warn 为 x=8/21/34/47）。排查：①三模型所有字母方块共用默认 texOffs(0,0)；②经 javap 反汇编 forge-1.20.1 srg jar 的 ModelPart$Cube 构造器字节码确认 vanilla 自动 UV 布局——正面(+z/SOUTH)采样区为 U∈[u+sz+sx+sz, u+sz+sx+sz+sx]、V∈[v+sz, v+sz+sy]；③故所有方块正面都采样纹理同一角落区域（x≈15~22），贴图上的字母从未被采样到。修复：按公式 `u=字母x−sz−sx−sz` 逐块设置 texOffs（ERROR 5 块 u=-14/-1/12/22/38、v=0；NULL 4 块 u=-4/9/19/32、v=6/6/7/7；WARN 4 块 u=-10/5/16/26、v=5；NULL/WARN 的 v 取 12−sz−(sy−6)/2 使 7px 字母行在面内居中），并加中文注释说明根因与计算方式；gradlew build BUILD SUCCESSFUL（36s），开发日志二、27 已追加
- 【新增唱片 friends_wine/air】素材「模型/唱片/」为两个 mp4 视频文件（friends_wine.mp4=朋友的酒 4:17、air.mp4=鸟之诗 5:50）+ 封面 png：①ffmpeg（run/package/ffmpeg.exe）`-vn` 提取音频轨转码 mono OGG（44.1kHz/vorb/160kbps），验证 ch=1、ticks=5145/7003；②注册 ModSounds music_friends_wine/music_air + ModItems 两张 RecordItem（comparator 复用 11/12，Rarity.RARE stacksTo 1）+ sounds.json（stream:true+subtitle）+ 2 个 item model + lang 中英（物品名/desc/subtitle）+ music_discs 标签 + 创造栏；③封面 png 复制为 textures/item/music_disc_<key>.png；④gradlew build BUILD SUCCESSFUL（30s）；开发日志已同步（目录 51、二、31 补充、三、2 物品总数 136/音乐唱片 32、三、4 声音 36）；注：ffmpeg 转 mp4 必须加 -vn 忽略视频轨，否则便携版会把 1080p 视频流转 theora 拖慢转码（首次即被中断产生残缺文件，已重转完整）

## 2026-09-09

- 检查 GitHub 仓库访问链路：github.com:443 直连被网络拦截（TCP 不通），api.github.com:443 与 ssh.github.com:443 可连通；gh CLI（HTTPS API）已登录 HaiOu-Redstone（token 含 repo/workflow），可正常读取仓库与 Release 列表
- 【修复】SSH 私钥 `id_ed25519` ACL 权限过宽（Authenticated Users 可修改、Users 可读），OpenSSH 拒绝加载（"permissions too open"）；已收紧为仅当前用户可访问（inheritance:r + 仅 lenovo:F）
- 验证 SSH over 443 链路：`ssh -T -p 443 git@ssh.github.com` 认证成功（Hi HaiOu-Redstone!）；`git ls-remote origin HEAD` 与 `git push --dry-run origin main` 均成功（HEAD=a82ccb1，Everything up-to-date）
- 注：普通沙箱模式下 git 调 ssh 时 sh.exe 无法创建信号管道（Win32 error 5），git 远程操作需在更宽权限下执行
- 测试 DSH IM 集成连通性：微信机器人（wx_ebbdf6261f88026fa9b3782a，iLink 长轮询）与 QQ 机器人（qq_9a003feee35f3e253b831b9e，官方 WebSocket）
  - 微信：getupdates 返回 200 + `{msgs:[], sync_buf, get_updates_buf}`，按插件判定逻辑（ret/errcode 非 0 才算失败）属连接正常；getconfig 报 ret=-4（typing ticket RPC 失败，仅影响"正在输入"提示，不影响收发消息）
  - QQ：`/app/getAppAccessToken` 获取 access_token 成功（expires_in≈6673s），appId/secret 有效，WebSocket 通道可用
  - 注：本机 curl/PowerShell 走 Windows schannel 时 TLS 凭据加载失败（SEC_E_NO_CREDENTIALS，沙箱限制），改用 DSH 自带 Node.js（内置 OpenSSL）测试成功
- QQ 机器人绑定本对话测试（session-f41f2f27-4495-418e-8349-0d60c64ad4c3，即当前 Web 对话）：
  - 通过浏览器会话 cookie（.credentials.yaml 的 browser-session 签名）认证 dsh web，可调用插件 RPC 通道 `/qq`（如 connection.status，返回 connected/healthy，WebSocket 长连接正常）
  - 通过 QQ 开放平台 API（api.sgroup.qq.com，Authorization: QQBot token）向 owner 主动发送 6 条测试消息（/session 命令、文本、/status、/help、/m、中文问答），全部发送成功（返回消息 ID）
  - 发现：QQ 会话绑定存于 `config/integrations/dsh-qq/bots/*/state.json` 的 sessions 映射（当前绑到旧会话 session-34edae99）+ 插件内存缓存；插件只在收到**入站**消息时处理 `/session` 命令，主动推送（出站）消息不会触发绑定重写。需在 QQ 内给机器人发 `/session session-f41f2f27-4495-418e-8349-0d60c64ad4c3` 完成绑定的持久化
- QQ 绑定成功与对话链路验证：
  - 用户在 QQ 内发送 `/session session-f41f2f27-4495-418e-8349-0d60c64ad4c3` 后，插件已把 `c2c:A7AE7163...` 绑定改写为当前会话 session-f41f2f27（state.json 17:47 更新，workspaces.json 同步切到 E:\trae\program\CC_RC）
  - 会话日志确认两条 **QQ 入站消息**成功进入本对话（source.kind=user, rpcId 前缀 `qq-`）：「对话测试」（seq 85971）与「文件测试，请将该文件放至工作目录/模型/错误生物(新建一个文件夹)」（seq 89358）
  - 文件测试：QQ 消息仅含文本、无文件附件（无 msgElements/attachments/files 字段），工作区无 `.dsh-im/inbound` 暂存文件；已新建目录 `模型/错误生物/` 待放入文件，需用户在 QQ 侧以文件消息重发或说明"该文件"的具体位置
- 【文件链路验证成功】用户通过 QQ 发送文件 error.zip（14,273 字节），入站暂存于 `.dsh-im/inbound/20260909-180447-qSMDNm/01-error.zip`（插件按时间戳命名目录），内容为 3D 模型：error/error.obj（66,104B）+ error/error.mtl（103B）+ error/texture.png（145B）
  - 已按用户要求解压存放至 `模型/错误生物/`：error/error.obj、error/error.mtl、error/texture.png，并保留原始 error.zip 副本；整个 QQ 文件消息链路（QQ → 插件 → .dsh-im/inbound → 工作区）验证通过
- QQ **群聊**链路验证：state.json 出现群聊绑定 `group:47BF966A42FE0B3C904771CEB48A60FA → session-f41f2f27`（与私聊同一会话）；群聊消息（含用户"该对话为群聊对话，是否正常？"）均成功入站进入本对话（rpcId 前缀 `qq-`，现已累计 4 条 QQ 入站消息）；按插件设计群聊仅在 `GROUP_AT_MESSAGE_CREATE`（被 @ 时）才应答（qq-bridge.mjs addressed 判定），群聊访问策略为 open + 默认允许执行命令，行为正常
- 【群聊行为准则】用户指示：QQ 群里其他人发消息若包含不合理/玩笑/不正经请求，一律当作玩笑处理（不执行）；若包含正当的开发请求，可以执行。本条已作为群聊交互的行为基准，后续处理群聊消息时遵守
- 【QQ 播报守护脚本】新增 `qq_broadcast.mjs` + `start_qq_broadcast.bat`（DSH 内置 Node 运行）：
  - 功能 1 阶段思考摘要：增量读取会话日志 `.jsonl.zstd`（按 zstd 魔数切帧、记录字节偏移、多行帧拆分），聚合每步 reasoning/text 增量，在 step/end 边界按 3s 窗口合并后发「🧠 思考进展」摘要到 QQ；
  - 功能 2 发问转发：识别 `ask_user_question` 工具调用与 `approval/asked` 事件，把问题原文推送 QQ（❓ 需要你确认/补充信息）；
  - 功能 3 错误通知：识别 `turn/end` 的 error/stopped/blocked reason，推送「⚠️ 回合中断/出错」；
  - 实测：hello 上线消息经 QQ 开放平台 API 发送成功（**私聊 ok**）；**群聊主动推送被 QQ 平台拒绝（40034105 主动消息无权限）**——QQ 群机器人只能由用户 @ 触发的会话内回复（插件原链路负责），故守护默认仅私聊播报（BROADCAST_GROUP=1 可尝试群聊，失败自动跳过）；
  - 修复：帧解析从"单帧=单 JSON"改为按行拆分（帧可能含多行 JSON）；BigInt 偏移与 Number 混用已统一
- 【群聊权限恢复】用户之前在 QQ 开放平台重设了机器人权限：群聊主动推送已恢复（实测 `POST /v2/groups/{group_openid}/messages` 返回 HTTP 200 + 消息 ID，此前 40034105 无权限问题已消除）；已更新 `qq_broadcast.mjs` 默认同时播报私聊 + 群聊，hello 上线消息两个目标均发送成功
- 【@ 无回应排查结论】QQ 插件状态 connected/healthy，群聊与私聊绑定完好、访问策略 open 正常；会话日志确认 turn 15（"审查当前工作区项目"）的 `TURN_INTERRUPTED` 实为**用户主动停止**（turn/end kind=aborted, reason=user，发生于"你去把token用完"指令之后），非插件故障；此后 turn 17/18/19 全部正常 completed，@ 触发链路验证正常
- 【错误生物实体 error_mob】用户通过 QQ 发送的「模型/错误生物/error.obj」（扁平 ERROR 字样，5 部件、32x32 贴图）立体化做成敌对生物实体：  - 新增 `entity/ErrorMob.java`：继承 Monster，AI 完全复制原版蠹虫（Silverfish）寻路与攻击逻辑（FloatGoal→MeleeAttackGoal→WaterAvoidingRandomStrollGoal→LookAtPlayerGoal→RandomLookAroundGoal→HurtByTargetGoal→NearestAttackableTargetGoal），**无任何方块交互**（不继承银鱼类、无虫蚀/藏匿/召唤）；属性生命 8 / 速度 0.25 / 攻击 3；**不注册 SpawnPlacements，不自然生成**，只能刷怪蛋/刷怪笼（MobCategory.MONSTER 刷怪笼原生支持）
  - 模型 `entity/ErrorMobModel.java`：OBJ 5 个字母部件 z 厚度 0.05→0.35 方块立体化（LayerDefinition，宽约 2.1×高 1.04 方块，脚底中心原点）；贴图 `textures/entity/error_mob.png`（64x32 深灰底+红色 ERROR，脚本生成）；渲染器 `entity/ErrorMobRenderer.java`（缩放 0.45 匹配碰撞箱）
  - 注册：ModEntities ERROR_MOB（sized 0.6×1.5，MobCategory.MONSTER）、ModItems 刷怪蛋 error_mob_spawn_egg（主色 0xDC2828/次色 0x1E1E28 + 双色点纹贴图 + item/generated 模型）、CcRc 注册 EntityAttributeCreationEvent + 客户端渲染器/模型层、lang 中英文、主创造标签
  - 构建验证：gradlew compileJava 通过（SpawnEggItem 构造需传 EntityType 而非 Supplier，已修复）；gradlew build 成功产出 cc_rc-0.0.8.jar；开发日志已同步（目录 45、二、27 错误生物、三、2 物品 121 个/特殊物品 24、三、6 实体 2 个）
- 【守护进程自动启动 + 修复 ESM 崩溃】用户要求"打开软件/打开对话时自动启动"守护（非开机自启）：
  - 实现：web profile 新增启动插件 `config/profiles/web/ccrc-broadcast.mjs`，通过 `cordis.patch.yml` insert（`name: ./ccrc-broadcast.mjs`）加载；dsh web 启动 3 秒后自动 spawn `qq_broadcast.mjs --hello`（detached，独立存活）
  - 【错误打断根因】① 初版插件误用 `.js` 扩展名（ESM 语法 `export`），而 web profile 的 package.json **无 `type: module`** → 按 CommonJS 解析抛 `SyntaxError` → **dsh web 启动崩溃、60892 端口不可达**；② `cordis.patch.yml` 初版带 UTF-8 BOM（PowerShell 写入）可能干扰解析
  - 【修复】插件改名 `.mjs`（强制 ESM 解析）+ patch 用无 BOM UTF-8 重写 + 删除错误 `.js` 文件；修复后 dsh web 冒烟测试正常启动、插件成功加载
  - 【遗留限制】DSH 沙箱（workspace-write）禁止 Node `spawn` 子进程（EPERM）——守护无法从沙箱内被 spawn；桌面应用正常运行环境不受沙箱限制，插件应在真实环境正常拉起守护。待用户在桌面应用打开对话后验证守护自动启动
- 【功能全面复检】对当前实现做只读完整性验证：qq_broadcast.mjs / ccrc-broadcast.mjs（web 与工作区）语法均通过、插件可正常 import（exports: apply）；播报仅取 textBuf（`📝 助手` 前缀，不含 reasoning 碎片）、看门狗（WATCHDOG_TIMEOUT_MS=5min / TICK=10s）、提问/审批带 "@机器人" 提示、approval/decided 与 tool/result 清除逻辑均存在；错误生物 3 个 Java 类 + 2 张贴图 + lang 条目 + cc_rc-0.0.8.jar 齐全；守护脚本本体运行测试正常（启动/监听日志/参数生效/无错误）；已清理残留临时脚本（read_png.mjs / retest_perms.mjs）
- 【修复客户端启动崩溃】用户启动客户端时崩溃：`NullPointerException: Registry Object not present: cc_rc:error_mob`（crash-2026-09-10_19.20.03-client.txt，栈在 ModItems.java:536）
  - 根因：刷怪蛋 `ERROR_MOB_SPAWN_EGG` 构造时直接 `ModEntities.ERROR_MOB.get()`——物品注册阶段实体注册尚未完成；且 CcRc 构造函数中 `ModItems.ITEMS.register` 排在 `ModEntities.ENTITY_TYPES.register` 之前，注册顺序颠倒
  - 修复①：刷怪蛋改用 `ForgeSpawnEggItem`（`net.minecraftforge.common.ForgeSpawnEggItem`，接受 Supplier 惰性解析实体，common_setup 阶段才 get()）
  - 修复②：CcRc 注册顺序调整——`ModEntities.ENTITY_TYPES` 移到 `ModItems.ITEMS` 之前（实体先于物品）
  - 验证：gradlew compileJava 通过（仅剩既有 deprecation 警告）、gradlew build 成功，cc_rc-0.0.8.jar 已更新（19:57，109,728,212 字节）
- 【错误生物变种 null/warn】用户追加两个模型「模型/错误生物/null.zip」「WARN.zip」（扁平字牌 OBJ，同 error 格式），注册为独立实体 `error_mob_null` / `error_mob_warn`（仅模型/贴图不同，行为与主变种一致）：
  - 模型：`entity/ErrorMobNullModel.java`（"null" 字样 4 部件，宽约 0.8 方块）、`entity/ErrorMobWarnModel.java`（"WARN" 字样 4 部件，宽约 2.0 方块），z 厚 0.05→0.35 立体化；贴图 `textures/entity/error_mob_null.png`（暗紫灰）、`error_mob_warn.png`（琥珀黄）
  - 架构：新增共用基类 `entity/ErrorMobModelBase.java`（root + setupAnim 转身），三个模型类继承；`ErrorMobRenderer` 重构为构造注入模型工厂/贴图/缩放（NULL 0.75 / WARN 0.45 / ERROR 0.45）
  - 注册：ModEntities ERROR_MOB_NULL/WARN（共用 ErrorMob 类与属性，碰撞箱 0.6×0.6 / 0.6×1.2）；ModItems 两个新刷怪蛋（ForgeSpawnEggItem：null 0x8C82B4/0x282338、warn 0xE6AA3C/0x372D14）+ 双色点纹贴图 + item/generated 模型；CcRc 注册 3 个模型层定义 + 3 个渲染器（含 import 修复）；lang 中英文 + 主创造标签
  - 构建：gradlew compileJava 通过（一次 import 缺失已修）、gradlew build 成功，cc_rc-0.0.8.jar 更新（21:01，109,734,635 字节）；开发日志已同步（二、27 变种、三、2 物品 123/特殊物品 26、三、6 实体 4 个）
- 【/ccrc 指令归并】按用户要求把三个 CC: Tweaked 设备数量相关指令统一归到 `/ccrc peripheral` 分支下：`/ccrc peripheral get_count <类型>`（查询）、`/ccrc peripheral set_count <类型> <数字>`（OP 强制改值，写 ids.json + 反射同步内存）、`/ccrc peripheral list`（仅查看 ids.json 内容）；旧顶层 `/ccrc get_count / set_count / list` 保留为兼容别名指向同一实现；编译与 build 均通过，cc_rc-0.0.8.jar 更新（09-11 15:21，109,734,879 字节），开发日志已同步（目录 41、二、23）
- 【删除旧顶层指令】按用户要求删除旧顶层 `/ccrc get_count / set_count / list` 别名（设备数量指令仅保留 `/ccrc peripheral` 分支，避免指令重名）；同步更新类注释与 Javadoc；编译与 build 均通过，cc_rc-0.0.8.jar 更新（09-11 16:27，109,734,654 字节），开发日志已同步（目录 41、二、23）
- 【修复 3 档拉杆强充能信号 bug】用户反馈 console_lever_6/7 对后方强充能时信号恒为 15（应为二档 8 / 三档 15），相邻方块弱信号正常：
  - 根因：`ConsoleLever3StageBlock.getDirectSignal` 写死"挡位 > 0 → 15"，未按档位输出
  - 修复：改为调用 `getSignal` 按档位输出（二档 8 / 三档 15），仅在连接方向强充能，与按钮/刷卡机「强充能同值」模式一致；类注释更新
  - 验证：gradlew compileJava 通过、build 成功，cc_rc-0.0.8.jar 更新（09-11 16:44，109,734,674 字节）；开发日志已同步（二、5.2 修复记录）
- 【FAAS 服务器音效替换】用户通过 QQ 发送新音效文件 `server_loop_10_whisper.ogg`（455.9KB，经 ftn 链接下载验证为合法 OggS，约 20 秒 / 44100Hz / 双声道），要求替换 FAAS 服务器环境音效：
  - 替换 `src/main/resources/assets/cc_rc/sounds/server_noise.ogg`（原 8 秒 → 新 20 秒；播放逻辑为 loop=true 无缝循环，时长不影响听感，无需改代码）
  - 完整 build 通过，cc_rc-0.0.8.jar 更新（09-13 15:23，110,107,159 字节，体积 +约372KB 为新音效）；开发日志已同步（二、19 音效说明）
- 【编辑工具 edit_tool】用户要求注册编辑工具（模型「模型/其他物品/edit_tool.png」），可编辑「可显示名称的方块」表面文字：
  - **统一接口** `block/ITextDisplay.java`：定义 `setText/getText`，实现于 ConsolePanelBlockEntity（基类，其子类拉杆/圆盘记录仪自动继承）、DigitalDisplayBlockEntity、DigitalKnobBlockEntity、DigitalPlotterBlockEntity
  - **物品** `item/EditToolItem.java`：主手右键 ITextDisplay 方块 → `NetworkHooks.openScreen` 打开编辑菜单；副手放置可显示名称方块时经 `BlockEvent.EntityPlaceEvent` 检测 + 延迟 1 tick 自动打开编辑界面
  - **GUI** `gui/EditTextMenu.java`（无槽位纯文本菜单，携带方块坐标+当前文字，快速移动返回空）+ `gui/EditTextScreen.java`（类铁砧：借用 iron 背景图 + 单行 EditBox 最大 32 字符 + 「完成」按钮，Enter 提交）
  - **网络** `network/EditTextPacket.java`（C2S，index 2）：提交方块坐标+文本 → 服务端校验 ITextDisplay + 距离 8 格 → `setText`（空文本=清除显示）
  - **注册**：ModItems EDIT_TOOL（stacksTo 1）、ModMenuTypes EDIT_TEXT（IForgeMenuType 传坐标+文字）、CcRc 注册 MENU_TYPES + MenuScreens.register、物品模型/贴图/lang 中英文/主创造标签
  - 修复：EditTextMenu 补 `quickMoveStack`（无槽菜单返回空栈）、EditToolItem 的延迟用 `TickTask` 而非 Runnable
  - 构建：gradlew compileJava 通过（2 处修复后）、build 成功，cc_rc-0.0.8.jar 更新（09-13 17:39，110,120,898 字节）
- 【编辑工具 GUI 替换 + E 键修复】用户要求：① GUI 背景换为「模型/其他物品/edit_tool_gui.png」；② 修复按 E 键会关闭编辑界面：
  - 背景：`EditTextScreen` 弃用原版铁砧背景图，改用 `textures/gui/edit_tool_gui.png`（256x256，从模型目录复制，分析确认内容区约 175x198，全图渲染为 256x256 面板）；输入框/按钮按 256 面板居中布局（输入框 y+110、按钮 y+144、标题 y+88）
  - E 键修复：覆写 `keyPressed`，在 `super.keyPressed` 前用 `this.minecraft.options.keyInventory.matches(keyCode, scanCode)` 拦截背包键——编辑界面为纯输入界面，按 E 不应触发原版背包键关闭行为，仅 Esc 可关闭
  - 构建：gradlew compileJava 通过、build 成功，cc_rc-0.0.8.jar 更新（09-13 18:09，110,122,810 字节）；开发日志已同步（二、28 GUI 说明）
- 【编辑工具 GUI 布局修正】用户反馈：GUI 图是按原版铁砧 GUI 位置画的，只需换贴图不移动控件；并出现"编辑文字"/"物品栏"字样位置不对、原文字显示位置不对：
  - 布局恢复铁砧原位：`imageWidth=176/imageHeight=166`，背景 blit 画布左上 176x166 区域；输入框（12,34 宽152）、完成按钮（12,68）坐标不移动
  - 覆写 `renderLabels` 只画标题（左上角原位）+ "当前：xxx"显示在标题右侧同行；**不调用 super.renderLabels** 从而消除"物品栏"字样（无槽位界面不需要玩家物品栏标题）
  - 构建：gradlew compileJava 通过、build 成功，cc_rc-0.0.8.jar 更新（09-13 18:30，110,122,887 字节）；开发日志已同步（二、28）
- 【扩展红石继电器与总线】用户要求制作两个配套方块（贴图「模型/扩展红石继电器」：relay_bus + bus_top/bottom、relay_expansion + expansion_front，均 16x16）：
  - **扩展红石继电器 extended_relay**：完整方块、水平四向放置（front 贴图朝放置方向）、方块实体存 outputs[6]/inputs[6]（索引=CC ComputerSide）；side↔世界方向按**继电器自身 FACING** 映射（front/back/left/right/top/bottom）；isSignalSource + getSignal/getDirectSignal 转发实体输出，tick 刷新四周输入
  - **扩展红石继电器总线 relay_bus**：完整方块、六方向放置（同 redstone_sender）、canConnectRedstone；BE 提供 findRelay(distance)（沿自身 FACING 在 distance 格查找继电器，紧贴=1 最大 Config.relay_bus.max_distance 默认 16 范围 1~64）
  - **总线 CC 外设**（redstone_relay_bus，经 registerPeripheralProvider 注册）：isRelay(distance)、setOutput/getOutput、setAnalogOutput/getAnalogOutput、getInput/getAnalogInput；side 以目标继电器自身朝向为基准
  - 注册：ModBlocks/ModItems/ModBlockEntities 各 2 个、CcRc 外设 provider、模型/blockstate（继电器 4 向、总线 6 向）/贴图/lang 中英文/主创造标签
  - 构建：gradlew compileJava 通过、build 成功，cc_rc-0.0.8.jar 更新（09-13 20:11，110,142,119 字节）；开发日志已同步（目录 47、二、29，方块 107/物品 126/方块实体 14）
- 【修复总线 FACING 崩溃】用户 CC 电脑访问外设时报 `IllegalArgumentException: Cannot get property DirectionProperty(name=facing, values=[north,south,west,east]) as it does not exist in Block{cc_rc:relay_bus}`：
  - 根因：`ExtendedRelayBusBlockEntity.findRelay` 误用 `HorizontalDirectionalBlock.FACING`（四值）读取总线朝向，而总线注册的是 `BlockStateProperties.FACING`（六方向）——属性不匹配抛异常
  - 修复：改为读取 `ExtendedRelayBusBlock.FACING`（六方向）；继电器方块实体用 `HorizontalDirectionalBlock.FACING`（水平四向）正确无需改
  - 构建：gradlew compileJava 通过、build 成功，cc_rc-0.0.8.jar 更新（09-13 20:36，110,142,108 字节）；开发日志已同步（二、29 修复记录）
- 【修复总线识别不到继电器】用户反馈总线 isRelay(false) / 找不到继电器：
  - 根因：总线外设所有 Lua 方法（isRelay/setOutput/getOutput/setAnalogOutput/getAnalogOutput/getInput/getAnalogInput）未标注 `@LuaFunction(mainThread = true)`——CC 电脑在**独立线程**运行 Lua，外设方法默认在计算机线程执行；而 `findRelay` 访问主线程的 `Level`/方块实体（`level.getBlockEntity`、`level.isLoaded`）不可靠 → 返回 null → 识别不到
  - 修复：全部方法补 `@LuaFunction(mainThread = true)`（与数码显示器外设既有写法一致）；findRelay 无需改
  - 构建：gradlew compileJava 通过、build 成功，cc_rc-0.0.8.jar 更新（09-13 20:57，110,142,130 字节）；开发日志已同步（二、29 第二条修复记录）
- 【修复继电器红石输出/输入不生效】用户反馈：① 方块需像玻璃一样自身不传导红石信号；② 设置输出后红石线无信号、读取输入读不到：
  - 输出无信号根因：`setOutput` 只更新 BE 内部 outputs 并同步 NBT，未通知邻居重新查询红石 → 修复：setOutput 变化时 `updateNeighborsAt`（含输出对侧）；同时修正 `getRedstoneOutput` 方向映射（MC 红石信号 direction 为反向语义，实际输出到 direction.getOpposite()，初版方向错位）
  - 输入读不到根因：tick 仅放置时调度一次，inputs 不持续刷新 → 修复：tick 持续自调度每 tick 刷新 + 新增 neighborChanged 立即刷新
  - 自身传导根因：canConnectRedstone 默认 true → 修复：继电器与总线均改 false（类似玻璃，红石线不连接/不穿过，信号仅由总线经 BE 读写）
  - 附带修复：`setOutput(int level)` 参数名与 BE 的 `Level level` 字段冲突导致编译错误，改名 `power` 并显式 `this.level`
  - 构建：gradlew compileJava 通过、build 成功，cc_rc-0.0.8.jar 更新（09-13 22:09，110,142,422 字节）；开发日志已同步（二、29 第三条修复记录）
- 【继电器红石行为对齐 CC RedstoneRelayBlock】用户反馈"充能问题仍未修复，参考原版玻璃或 CC 红石继电器"：
  - 对照 CC 官方 RedstoneRelayBlock 源码：`getSignal` 与 `getDirectSignal` 均返回**真实定向输出**（Block 层用 `incomingSide.getOpposite()` 反向，BE 内等价），`canConnectRedstone` 不覆写（默认 false）
  - 此前把 `getDirectSignal` 改为 0 属**过度修复**（导致继电器无法向目标方向强充能输出）；已恢复为与 `getSignal` 相同返回真实输出
  - 最终行为：`canConnectRedstone=false`（红石线不连接/不将其作为导体）、`getSignal=getDirectSignal` 定向真实输出、`isRedstoneConductor` 保持完整方块默认 true（实心阻断红石线跨格跳跃）——与 CC 继电器一致
  - 1 tick 延迟：CC `@LuaFunction(mainThread=true)` 固有机制（电脑线程→主线程下一 tick 执行），非 bug
  - 构建：gradlew compileJava 通过、build 成功，cc_rc-0.0.8.jar 更新（09-13 23:40，110,142,423 字节）；开发日志已同步（二、29 第四条修复记录，已更正为 CC 对齐方案）
